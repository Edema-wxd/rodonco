---
phase: 02-static-shop-ui
plan: 03
type: execute
wave: 2
depends_on:
  - 02-02
files_modified:
  - src/components/layout/Navbar.test.tsx
  - src/components/shop/OrderingClosedBanner.test.tsx
  - src/components/shop/ProductCard.test.tsx
  - src/lib/shop/products.test.ts
autonomous: true
requirements:
  - CART-04
must_haves:
  truths:
    - "Navbar cart icon renders consistently and cart badge only appears after hydration when itemCount > 0."
    - "Banner formatting and card price formatting are protected by automated tests."
  artifacts:
    - path: "src/components/layout/Navbar.test.tsx"
      provides: "CART-04 regression coverage (badge + hydration guard)"
    - path: "src/components/shop/OrderingClosedBanner.test.tsx"
      provides: "SHOP-03 banner contract coverage (open/closed + date format)"
    - path: "src/components/shop/ProductCard.test.tsx"
      provides: "SHOP-04 card contract coverage (From ₦… formatting + link)"
    - path: "src/lib/shop/products.test.ts"
      provides: "SHOP-02/04 price helper coverage (min variant price logic)"
  key_links:
    - from: "src/components/layout/Navbar.tsx"
      to: "src/hooks/useHasHydrated.ts"
      via: "hydration guard"
      pattern: "useHasHydrated"
---

<objective>
Add fast, targeted Vitest coverage for the key Phase 2 contracts, including CART-04.

Purpose: Ensure SHOP-03 banner formatting, SHOP-04 price display/link behavior, and CART-04 hydration guard regressions are caught automatically.
Output: A small set of `.test.ts(x)` files runnable via `npm test`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/02-static-shop-ui/02-CONTEXT.md
@.planning/phases/02-static-shop-ui/02-UI-SPEC.md

@src/components/layout/Navbar.tsx
@src/store/cart.ts
@src/hooks/useHasHydrated.ts
@src/components/shop/OrderingClosedBanner.tsx
@src/components/shop/ProductCard.tsx
@src/lib/shop/products.ts
@vitest.config.ts
@src/test/setup.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add CART-04 regression tests for Navbar cart icon + hydration-guarded badge</name>
  <read_first>
    - .planning/REQUIREMENTS.md (CART-04)
    - src/components/layout/Navbar.tsx (hasHydrated + itemCount behavior)
    - src/store/cart.ts (how to seed cart items for tests)
    - src/hooks/useHasHydrated.ts (how hydration is detected)
    - src/test/setup.ts (localStorage polyfill + clearing)
  </read_first>
  <files>
    src/components/layout/Navbar.test.tsx
  </files>
  <action>
    Create `src/components/layout/Navbar.test.tsx` using Vitest + Testing Library patterns already used in the repo.
    Test behaviors:
    - Badge is NOT rendered when `useHasHydrated()` is false even if the cart has items (pre-hydration safety).
    - Badge renders with the correct count when `useHasHydrated()` is true and the cart has N items.
    Use module mocking to control `useHasHydrated` return value; seed Zustand cart store state via its public API (avoid reaching into internals).

    Note: This is **test-only** coverage for CART-04. Do NOT modify `src/components/layout/Navbar.tsx` implementation behavior in this plan.
  </action>
  <acceptance_criteria>
    - Tests cover both pre-hydration and post-hydration badge behavior.
    - `npm test` passes locally.
  </acceptance_criteria>
  <verify>
    <automated>npm test</automated>
  </verify>
  <done>
    CART-04 is protected by an automated regression test.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add banner + product-card contract tests for Phase 2 formatting and routing</name>
  <read_first>
    - .planning/phases/02-static-shop-ui/02-CONTEXT.md (D-09, D-18, D-19)
    - src/components/shop/OrderingClosedBanner.tsx (banner behavior + date formatting)
    - src/components/shop/ProductCard.tsx (From ₦ formatting + Link target/scroll=false)
  </read_first>
  <files>
    src/components/shop/OrderingClosedBanner.test.tsx
    src/components/shop/ProductCard.test.tsx
  </files>
  <action>
    Create two test files:
    - `OrderingClosedBanner.test.tsx`
      - Asserts it returns null when `isOpen=true`.
      - Asserts it renders when `isOpen=false` and includes the exact string prefix `Ordering is closed. Next delivery:` and a formatted day/month form for a known ISO date (use a fixed ISO date).
      - Asserts `role="alert"` is set on the banner root.
    - `ProductCard.test.tsx`
      - Asserts it renders `From ₦` with the formatted numeric amount for a fixed kobo value.
      - Asserts the CTA renders text `Add to Order`.
      - Asserts the link points to `/shop/{product.id}` and preserves `scroll={false}` behavior (at minimum, confirm the `href`).
  </action>
  <acceptance_criteria>
    - Tests pin the locked formatting behaviors (D-09, D-18, D-19) so regressions fail loudly.
    - `npm test` passes.
  </acceptance_criteria>
  <verify>
    <automated>npm test</automated>
  </verify>
  <done>
    SHOP-03 and SHOP-04 formatting contracts have automated coverage.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add server helper tests for starting price (MIN variant price) aggregation logic</name>
  <read_first>
    - src/lib/shop/products.ts (new helper from Plan 02)
    - drizzle/schema.ts (fields used)
    - src/lib/db/index.ts (db access patterns; whether tests should mock db)
  </read_first>
  <files>
    src/lib/shop/products.test.ts
  </files>
  <action>
    Add `src/lib/shop/products.test.ts` testing the new “active products with starting price” helper:
    - Use dependency injection or module mocking to avoid requiring a real DB connection (unit-test the transformation/aggregation logic).
    - Include case: product with multiple variants returns the MIN price; products filtered by `is_active=true`.
    If the helper is implemented as a single Drizzle query, mock the `db.select().from().where()` chain to return representative rows and assert the mapping.
  </action>
  <acceptance_criteria>
    - Tests demonstrate min-price correctness and active filtering.
    - No network/DB required to run tests.
  </acceptance_criteria>
  <verify>
    <automated>npm test</automated>
  </verify>
  <done>
    The Phase 2 starting-price computation is covered by unit tests.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test runner → mocked modules | Tests may mock hooks/stores; ensure mocks are restored between tests to avoid false positives. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|----------|----------|-----------|-------------|-----------------|
| T-02-07 | R (Repudiation) | Test isolation | mitigate | Reset/restore mocks between tests; rely on `beforeEach` + `vi.restoreAllMocks()`. |
| T-02-08 | D (DoS) | Test runtime | mitigate | Keep tests unit-level (no DB); ensure `npm test` remains < 60s. |
</threat_model>

<verification>
- `npm test` passes.
</verification>

<success_criteria>
- CART-04 has explicit automated coverage.
- Banner and product-card formatting behaviors are pinned by tests.
- Starting price helper logic is protected against regressions.
</success_criteria>

<output>
After completion, create `.planning/phases/02-static-shop-ui/02-03-SUMMARY.md`
</output>

