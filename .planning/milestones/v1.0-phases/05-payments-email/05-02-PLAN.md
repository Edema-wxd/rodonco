---
phase: 05-payments-email
plan: 02
type: execute
wave: 3
depends_on:
  - 05-01
  - 05-03
files_modified:
  - src/app/(customer)/checkout/page.tsx
  - src/components/checkout/CheckoutExperience.tsx
  - src/components/checkout/CheckoutCartSummary.tsx
  - src/components/checkout/CheckoutExperience.test.tsx
autonomous: true
requirements:
  - CHKT-01
  - CHKT-03
  - CHKT-05
must_haves:
  truths:
    - "Ordering closed shows blocked state identical to Phase 3 semantics (reuse OrderingClosedBanner) (CHKT-03)"
    - "Ordering open renders two-column layout per 05-UI-SPEC — form left / cart summary right; stacked on mobile (CHKT-01)"
    - "Empty cart redirects to `/shop` (client navigates after hydrated cart read — CONTEXT D-02)"
    - "Pay Now disabled + spinner during init in-flight (CONTEXT D-03)"
    - "Paystack inline opens with access_code from `POST /api/orders/init` response (requires 05-03 merged first)"
---

<objective>

Replace the `/checkout` stub with the full **checkout experience UI**: gated by `getOrderingConfig()`, two-column responsive layout (`05-UI-SPEC`), RHF+Zod wired to `checkoutPayloadSchema`, cart summary from Zustand with hydration guard, empty-cart redirect, Pay Now loading state, and Paystack popup wiring via `@paystack/inline-js` using the real **`POST /api/orders/init`** contract (depends on Plan **05-03**).

Purpose: Delivers customer-visible CHKT-01/03/05 UI and client-side payment affordances.

</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-payments-email/05-CONTEXT.md
@.planning/phases/05-payments-email/05-UI-SPEC.md
@src/app/(customer)/checkout/page.tsx
@src/store/cart.ts
@src/hooks/useHasHydrated.ts
@src/lib/checkout/schemas.ts
</context>

<threat_model>

| Boundary | Description |
|----------|-------------|
| Browser | PUBLIC Paystack key only; never expose `PAYSTACK_SECRET_KEY`. |

| Threat | Severity | Mitigation |
|--------|-----------|-------------|
| Double submit | Medium | Disable Pay Now until init completes; loading state. |

</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Server shell + Closed state</name>
  <files>src/app/(customer)/checkout/page.tsx</files>
  <action>Maintain server-side ordering closed branch. For open state, render `<CheckoutExperience />` client island with `dynamic = "force-dynamic"` on page if needed.</action>
  <verify>npx tsc --noEmit</verify>
</task>

<task type="auto" tdd="true">
  <name>Checkout client + cart summary + Paystack hookup</name>
  <files>src/components/checkout/CheckoutExperience.tsx, src/components/checkout/CheckoutCartSummary.tsx, src/components/checkout/CheckoutExperience.test.tsx</files>
  <action>Implement form, terms, toast on cancel (CONTEXT D-04), clearCart on success before `router.push` (D-08). Call real `/api/orders/init` once 05-03 exists.</action>
  <verify>npm run test -- src/components/checkout</verify>
</task>

</tasks>

<verification>npx tsc --noEmit && npm run test -- src/components/checkout</verification>

<output>Create `05-02-SUMMARY.md` after execution.</output>
