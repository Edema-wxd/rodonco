---
phase: 05-payments-email
plan: 06
type: execute
wave: 5
depends_on: []
files_modified:
  - src/components/order/OrderConfirmationView.tsx
autonomous: true
requirements:
  - CONF-02
gap_closure: true
must_haves:
  truths:
    - "Order confirmation page greets the customer by name (e.g. 'Thank you, Jane Doe') before the order card"
---

<objective>
Render `order.customer_name` in the order confirmation page.

Purpose: CONF-02 and Roadmap SC-5 both require customer name to be displayed on the confirmation page. The data is available in the `Order` prop passed to `ConfirmedOrder` but is never rendered — a straightforward omission with a one-line fix.
Output: `OrderConfirmationView.tsx` updated so the `ConfirmedOrder` component shows a personalised greeting heading above the order card.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-payments-email/05-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add customer name greeting to ConfirmedOrder component</name>
  <files>src/components/order/OrderConfirmationView.tsx</files>
  <read_first>
    - src/components/order/OrderConfirmationView.tsx
  </read_first>
  <action>
In `ConfirmedOrder` (line 97), add a personalised greeting `<p>` element immediately after the success header `<div>` (the flex row containing `CheckCircle` and `<h1>Order Confirmed</h1>`), before the `<Card>`.

Insert after the closing `</div>` of the success header block (after line 108):

```tsx
{/* Customer greeting */}
<p className="mb-6 text-muted-foreground">
  Thank you, <span className="font-semibold text-foreground">{order.customer_name}</span>. Your order is confirmed and will be delivered on the date below.
</p>
```

No other changes to the file. `order.customer_name` is already available in the `ConfirmedOrderProps` interface via the `order: Order` prop — no prop changes needed.
  </action>
  <verify>
    <automated>grep -n "customer_name" /Users/mac/Documents/GitHub/rodonco/src/components/order/OrderConfirmationView.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep customer_name src/components/order/OrderConfirmationView.tsx` returns at least one match showing `order.customer_name` rendered in JSX
    - The greeting appears visually before the `<Card>` (reference + delivery + line items)
    - No TypeScript errors: `order.customer_name` is typed `string` on the `Order` type
  </acceptance_criteria>
  <done>`order.customer_name` is rendered in the `ConfirmedOrder` component as a greeting paragraph. CONF-02 satisfied.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DB → component | `order.customer_name` originates from a DB row fetched server-side; no client input path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-06-01 | Tampering | OrderConfirmationView.tsx | accept | customer_name is fetched from DB (server-side), not from URL params or client input; React escapes string output by default preventing XSS |
</threat_model>

<verification>
After task completes:

```bash
grep -n "customer_name" src/components/order/OrderConfirmationView.tsx
```

Must show `order.customer_name` in JSX context (not just a comment). TypeScript build must pass:

```bash
npx tsc --noEmit
```
</verification>

<success_criteria>
- `order.customer_name` is rendered in `ConfirmedOrder` as a visible greeting
- No TypeScript errors introduced
- CONF-02 requirement satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/05-payments-email/05-06-SUMMARY.md` following the summary template.
</output>
