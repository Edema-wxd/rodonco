---
phase: 05-payments-email
plan: 05
type: execute
wave: 4
depends_on:
  - 05-03
  - 05-04
files_modified:
  - src/app/(customer)/order/[ref]/page.tsx
  - src/lib/orders/getOrderForConfirmation.ts
  - src/components/order/OrderConfirmationView.tsx
autonomous: true
requirements:
  - CONF-01
  - CONF-02
  - CONF-03
must_haves:
  truths:
    - "`[ref]` param matches `orders.reference` textual lookup"
    - "Paid orders render confirmation layout per 05-UI-SPEC (reference, customer, line items, address, next delivery date)"
    - "Unknown ref or non-paid → clear error state + support CTA (CONTEXT D-10)"
---

<objective>

Ship **`/order/[ref]`** confirmation page: server-load order + items by **`reference`**, enforce **`paid`** gate for happy path vs explicit error UX for pending/failed lookups (CONF-01–03).

Purpose: Landing surface after Paystack success callback (paired with checkout client redirect).

</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-payments-email/05-UI-SPEC.md
@drizzle/schema.ts
@src/lib/db/index.ts
</context>

<threat_model>

| Threat | Severity | Mitigation |
|--------|-----------|------------|
| Reference enumeration | Low | MVP acceptable; rate-limit future |

</threat_model>

<tasks>

<task type="auto" tdd="false">
  <name>Loader helper</name>
  <files>src/lib/orders/getOrderForConfirmation.ts</files>
  <action>Join `orders` + `order_items` ordered consistently.</action>
  <verify>npx tsc --noEmit</verify>
</task>

<task type="auto" tdd="false">
  <name>Page + presentation component</name>
  <files>src/app/(customer)/order/[ref]/page.tsx, src/components/order/OrderConfirmationView.tsx</files>
  <action>Use `ordering_config.next_delivery_date` formatting consistent with storefront (reuse date helpers if exist).</action>
  <verify>npx tsc --noEmit</verify>
</task>

</tasks>

<verification>npx tsc --noEmit</verification>

<output>Create `05-05-SUMMARY.md` after execution.</output>
