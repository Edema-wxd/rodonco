---
phase: 05-payments-email
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - src/lib/checkout/schemas.ts
  - src/lib/checkout/cartToOrderDraft.ts
  - src/lib/checkout/schemas.test.ts
  - src/lib/checkout/cartToOrderDraft.test.ts
  - src/lib/paystack/initialize.ts
autonomous: true
requirements:
  - CHKT-02
gap_closure: false
must_haves:
  truths:
    - "checkoutPayloadSchema rejects invalid Nigerian phone, email, and empty terms (CHKT-02)"
    - "cartFingerprint / cart lines produce deterministic order_items drafts with integer kobo only"
    - "initializePaystackTransaction() calls Paystack REST with PAYSTACK_SECRET_KEY server-side only"
---

<objective>

Install Phase 5 payment dependencies (`@paystack/inline-js`, `resend`, `react-email`, `@react-email/components`, `nanoid`) and add server-safe helpers: checkout request Zod schema, CartItem→order draft + total (kobo), and a typed wrapper around Paystack **`POST /transaction/initialize`** using `reference` that will be stored as **`orders.reference`**.

Purpose: Gives later plans shared types/helpers so `/api/orders/init` and the webhook both treat **`orders.reference`** as the canonical Paystack reference (there is **no** `paystack_reference` column).

</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-payments-email/05-CONTEXT.md
@.planning/phases/05-payments-email/05-RESEARCH.md
@.planning/phases/05-payments-email/05-UI-SPEC.md
@drizzle/schema.ts
@src/types/index.ts
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → /api | Checkout JSON can be malformed or malicious — Zod is first gate (T-05-W01). |
| Server → Paystack | Secret key must never leak to browser; only `NEXT_PUBLIC_PAYSTACK_PUBLIC_KEY` is public. |

| Threat | Severity | Mitigation |
|--------|-----------|-------------|
| T-05-W01 | High | Strict Zod schemas; Nigerian phone normalization/validation regex per project precedent. |

</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Dependency + env alignment</name>
  <files>package.json, package-lock.json</files>
  <action>Add npm deps for Paystack inline, Resend, React Email, nanoid (+ dev types if `@types/nanoid` not needed — nanoid ships types).</action>
  <verify>npx tsc --noEmit</verify>
</task>

<task type="auto" tdd="true">
  <name>Schemas + cart draft helpers</name>
  <files>src/lib/checkout/schemas.ts, src/lib/checkout/schemas.test.ts, src/lib/checkout/cartToOrderDraft.ts, src/lib/checkout/cartToOrderDraft.test.ts</files>
  <action>Implement checkoutPayloadSchema (name, email, Nigerian phone Zod rule, delivery_address, allergy optional, terms checkbox). Implement buildOrderItemsDraftFromCart / cart fingerprint per CONTEXT D-05 (document chosen fingerprint format in code comment).</action>
  <verify>npm run test -- src/lib/checkout</verify>
</task>

<task type="auto" tdd="false">
  <name>Paystack initialize wrapper</name>
  <files>src/lib/paystack/initialize.ts</files>
  <read_first>Paystack REST initialize docs</read_first>
  <action>`import "server-only"`; POST with PAYSTACK_SECRET_KEY; pass amount (kobo), email, currency NGN, reference; return access_code/data needed by client Inline JS.</action>
  <verify>npx tsc --noEmit</verify>
</task>

</tasks>

<verification>- `npm run test` green (checkout helpers)
- `npx tsc --noEmit`</verification>

<output>Create `05-01-SUMMARY.md` after execution.</output>
