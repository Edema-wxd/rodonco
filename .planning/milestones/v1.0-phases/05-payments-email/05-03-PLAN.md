---
phase: 05-payments-email
plan: 03
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - src/app/api/orders/init/route.ts
  - src/lib/orders/findPendingReuse.ts
  - src/app/api/orders/init/route.test.ts
autonomous: true
requirements:
  - CHKT-04
  - INFRA-04
must_haves:
  truths:
    - "Route enforces ordering open via DB read consistent with storefront (reuse getOrderingConfig / no stale cache)"
    - "Creates or reuses pending order per CONTEXT D-05 before calling Paystack initialize"
    - "Returns JSON: reference, access_code (or shape required by checkout client), amount kobo"
    - "401/400 on invalid body; never leaks stack traces"
---

<objective>

Implement **`POST /api/orders/init`**: validate cart + checkout payload, resolve **`week_of`** from `ordering_config.next_delivery_date`, upsert **`orders` + `order_items`** in `pending` state with unique **`reference`**, call Paystack initialize, return data for inline JS.

Purpose: Backing API for CHKT-04 — persisted pending order **before** customer pays.

</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@drizzle/schema.ts
@src/lib/db/index.ts
@src/lib/shop/orderingConfig.ts
@src/lib/checkout/schemas.ts
@src/lib/checkout/cartToOrderDraft.ts
@src/lib/paystack/initialize.ts
</context>

<threat_model>

| Boundary | Description |
|----------|-------------|
| Public internet | Endpoint is unauthenticated customer traffic — abuse/rate-limit is Phase 6+; MVP: validate strictly + sane payload size. |

| Threat | Severity | Mitigation |
|--------|-----------|------------|
| Forged totals | Critical | Never trust client totals — recompute from cart lines + server truth for prices (join `products` / variants if needed for tamper resistance). |

</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Pending reuse helper</name>
  <files>src/lib/orders/findPendingReuse.ts</files>
  <action>Query pending order by email + cart fingerprint; document limits (if none, create new).</action>
  <verify>npm run test -- src/app/api/orders/init</verify>
</task>

<task type="auto" tdd="true">
  <name>Route handler</name>
  <files>src/app/api/orders/init/route.ts, src/app/api/orders/init/route.test.ts</files>
  <action>POST-only; parse JSON body; combine checkout fields + serialized cart lines; Drizzle transactional inserts where possible.</action>
  <verify>npm run test -- src/app/api/orders/init</verify>
</task>

</tasks>

<verification>npm run test -- src/app/api/orders/init && npx tsc --noEmit</verification>

<output>Create `05-03-SUMMARY.md` after execution.</output>
