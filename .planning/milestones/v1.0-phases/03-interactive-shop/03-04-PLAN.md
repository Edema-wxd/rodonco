---
phase: 03-interactive-shop
plan: 04
type: execute
wave: 2
depends_on:
  - 03-02
files_modified:
  - src/lib/shop/products.ts
  - src/components/shop/ShopGrid.tsx
  - src/app/(customer)/shop/page.tsx
  - src/app/(customer)/shop/default.tsx
autonomous: true
requirements:
  - SHOP-05
  - SHOP-11
must_haves:
  truths:
    - "Navigating to `/shop/[id]` opens the drawer route without a full reload (SHOP-05)"
    - "Hard refresh of `/shop/[id]` still renders the shop grid behind the drawer via `shop/default.tsx` (D-01)"
    - "Back button closes the drawer to `/shop` and preserves scroll position by using `scroll={false}` on navigation (D-03)"
    - "When ordering is closed, add-to-cart entry points on the shop grid are disabled and a sticky banner is shown (D-12/D-13, SHOP-11)"
  artifacts:
    - path: "src/app/(customer)/shop/default.tsx"
      provides: "children-slot fallback to keep grid behind drawer on refresh"
    - path: "src/components/shop/ShopGrid.tsx"
      provides: "Server-rendered shop grid with product links to `/shop/[id]`"
    - path: "src/lib/shop/products.ts"
      provides: "Server-only product loader for shop grid"
  key_links:
    - from: "src/components/shop/ShopGrid.tsx"
      to: "src/app/(customer)/shop/@drawer/[slug]/page.tsx"
      via: "Link href=/shop/${id}"
      pattern: "href=.*\\/shop\\/"
    - from: "src/app/(customer)/shop/default.tsx"
      to: "src/components/shop/ShopGrid.tsx"
      via: "renders grid on refresh behind drawer"
      pattern: "ShopGrid"
---

<objective>
Build the real `/shop` grid and routing contracts for the drawer overlay: server-render the product sections from DB, link each product to `/shop/[id]` (treating `slug` as product id), add a `shop/default.tsx` fallback so refreshing a deep-link keeps the grid behind the drawer, and enforce ordering-closed disablement + sticky banner.

Purpose: This establishes the URL + refresh semantics and the interactive entry point required for the drawer UX.
Output: Shop grid server component + DB loader + `default.tsx` refresh fallback.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-interactive-shop/03-CONTEXT.md
@drizzle/schema.ts
@src/lib/db/index.ts
@src/lib/shop/orderingConfig.ts
@src/components/shop/OrderingClosedBanner.tsx
@src/app/(customer)/shop/layout.tsx
@src/app/(customer)/shop/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add server-only shop product loader</name>
  <files>src/lib/shop/products.ts</files>
  <read_first>
    - Read `drizzle/schema.ts` for tables/columns.
    - Read `src/lib/db/index.ts` to use canonical DB client.
  </read_first>
  <action>
    - Create `src/lib/shop/products.ts` as a server-only module (`import "server-only"`).
    - Export `getActiveProductsForShop()` that fetches `products WHERE is_active=true`, ordered by created_at or name.
    - Return data in the existing `Product` shape from `src/types/index.ts` (or a compatible serialized subset).
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run build</automated>
  </verify>
  <done>
    - Server-only module compiles and returns active products.
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement ShopGrid server component with deep-link navigation and ordering-closed UI</name>
  <files>src/components/shop/ShopGrid.tsx</files>
  <read_first>
    - Read `03-CONTEXT.md` decisions D-01/D-03/D-12/D-13.
    - Read `OrderingClosedBanner.tsx` to reuse banner.
  </read_first>
  <action>
    - Create a server component `ShopGrid` that:
      - Calls `getActiveProductsForShop()` and `getOrderingConfig()`.
      - Renders two sections: Fresh Produce and Cooking Kits (filter by `product.type`).
      - Shows the sticky `OrderingClosedBanner` when ordering is closed (D-13).
      - Renders each product as a card linking to `/shop/{product.id}` with `scroll={false}` so opening/closing preserves scroll (D-03).
      - Treat route param `slug` as `product.id` (schema has no slug column per research constraint).
      - Provide an “Add to order” affordance on cards (if present), but when ordering is closed disable it everywhere per D-12 (keep it non-interactive + clearly disabled).
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run build</automated>
  </verify>
  <done>
    - `/shop` renders real products grouped by type.
    - Clicking a product navigates to `/shop/[id]` without scrolling to top.
    - When ordering is closed, banner is visible and add-to-cart entry points are disabled.
  </done>
</task>

<task type="auto">
  <name>Task 3: Wire `/shop` page and `shop/default.tsx` refresh fallback to render the grid</name>
  <files>
src/app/(customer)/shop/page.tsx
src/app/(customer)/shop/default.tsx
  </files>
  <read_first>
    - Read `src/app/(customer)/shop/page.tsx` current stub.
    - Read `src/app/(customer)/shop/layout.tsx` to ensure slot behavior aligns with D-01.
  </read_first>
  <action>
    - Update `/shop/page.tsx` to render `ShopGrid`.
    - Add `src/app/(customer)/shop/default.tsx` (implicit children slot fallback) that also renders `ShopGrid`.
      - This is critical for hard-refresh on `/shop/[id]` to keep the grid behind the drawer (D-01).
    - Ensure both files are server components.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run build</automated>
  </verify>
  <done>
    - `shop/default.tsx` exists and renders the shop grid.
    - Refreshing a deep link `/shop/[id]` shows grid behind the drawer once drawer UI exists.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL params → DB query | Route uses user-controlled `id` to fetch product later; must handle not-found |
| server ordering config → UI affordances | Must not accidentally allow add-to-cart when closed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|----------|----------|-----------|-------------|-----------------|
| T-03-07 | T (Tampering) | ordering-closed add entry points | mitigate | Disable add-to-cart affordances when closed; show banner prominently (D-12/D-13) |
| T-03-08 | D (DoS) | deep-link refresh fallback | mitigate | Provide `shop/default.tsx` fallback to prevent 404/blank background on refresh (D-01) |
</threat_model>

<verification>
- `npm run build` passes.
- Manual routing check:
  - Visit `/shop`, scroll mid-page, click a product → URL becomes `/shop/{id}` and scroll position is preserved behind drawer.
</verification>

<success_criteria>
- `/shop` is real and deep-links to `/shop/[id]` with correct refresh/scroll semantics and ordering-closed disablement.
</success_criteria>

<output>
After completion, create `.planning/phases/03-interactive-shop/03-04-SUMMARY.md`
</output>

