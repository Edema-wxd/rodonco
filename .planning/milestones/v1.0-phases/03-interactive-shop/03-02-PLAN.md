---
phase: 03-interactive-shop
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/shop/orderingConfig.ts
  - src/components/shop/OrderingClosedBanner.tsx
  - src/app/(customer)/checkout/page.tsx
autonomous: true
requirements:
  - SHOP-11
must_haves:
  truths:
    - "Ordering-open state is fetched from DB on every request (no caching assumptions)"
    - "Checkout is blocked with a clear message when ordering is closed"
  artifacts:
    - path: "src/lib/shop/orderingConfig.ts"
      provides: "Server-only ordering_config loader (id=1)"
    - path: "src/components/shop/OrderingClosedBanner.tsx"
      provides: "Sticky banner UI used across shop/drawer/cart"
    - path: "src/app/(customer)/checkout/page.tsx"
      provides: "Blocked checkout state when ordering is closed"
  key_links:
    - from: "src/app/(customer)/checkout/page.tsx"
      to: "src/lib/shop/orderingConfig.ts"
      via: "server read"
      pattern: "getOrderingConfig"
---

<objective>
Centralize ordering-window truth and enforce it at checkout: create a server-only accessor for `ordering_config` (row id=1), render a reusable sticky “ordering closed” banner, and block `/checkout` when ordering is closed.

Purpose: Phase-wide enforcement depends on a single source of truth and consistent UI messaging.
Output: `getOrderingConfig()` helper, banner component, and checkout blocked state.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-interactive-shop/03-CONTEXT.md
@drizzle/schema.ts
@src/lib/db/index.ts
@src/app/(customer)/checkout/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add server-only ordering config loader (no-cache by design)</name>
  <files>src/lib/shop/orderingConfig.ts</files>
  <read_first>
    - Read `src/lib/db/index.ts` to use the canonical Drizzle client.
    - Read `drizzle/schema.ts` to query `ordering_config` safely and correctly.
  </read_first>
  <action>
    - Create `src/lib/shop/orderingConfig.ts` and mark it server-only (use `import "server-only"`).
    - Export `async function getOrderingConfig()` that reads row `id=1` from `ordering_config`.
    - Handle missing row defensively (return a safe default object with `is_ordering_open: true` but also surface a clear log/error for debugging).
    - Ensure consumers can call this in server components without introducing client bundles.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run build</automated>
  </verify>
  <done>
    - `getOrderingConfig()` compiles and can be imported from a server component.
    - No client component imports this module (server-only boundary enforced).
  </done>
</task>

<task type="auto">
  <name>Task 2: Create sticky ordering-closed banner component</name>
  <files>src/components/shop/OrderingClosedBanner.tsx</files>
  <read_first>
    - Read Phase 3 decisions D-12/D-13 in `03-CONTEXT.md` for placement/behavior constraints.
  </read_first>
  <action>
    - Create a small component that renders nothing when `isOpen=true`, and renders a sticky banner when `isOpen=false`.
    - Banner should accept props for `isOpen`, and optionally `cutoffMessage` / `nextDeliveryDate` if available.
    - Keep styling consistent with existing Tailwind patterns; ensure it stays visible above shop content (sticky top, below navbar).
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run build</automated>
  </verify>
  <done>
    - Banner is safe to use in server or client trees (pure presentational component).
    - When `isOpen=false`, banner is sticky and readable on mobile and desktop.
  </done>
</task>

<task type="auto">
  <name>Task 3: Block `/checkout` when ordering is closed</name>
  <files>src/app/(customer)/checkout/page.tsx</files>
  <read_first>
    - Read `src/app/(customer)/checkout/page.tsx` current stub.
    - Read `src/lib/shop/orderingConfig.ts` created in Task 1.
  </read_first>
  <action>
    - Convert checkout page into a server component that calls `getOrderingConfig()`.
    - If `is_ordering_open=false`, render a clear full-width blocked state (no form UI) per requirements.
    - Include the sticky banner component in the blocked state to satisfy “banner on shop + drawer + cart” later; this ensures checkout also communicates closure clearly.
    - If `is_ordering_open=true`, keep the existing stub content (this phase does not build the checkout form).
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run build</automated>
  </verify>
  <done>
    - When ordering is closed, `/checkout` shows a blocked state and no interactive checkout UI.
    - When ordering is open, `/checkout` still renders without errors.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server DB → rendered UI | Ordering state must reflect DB truth and not be accidentally cached |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|----------|----------|-----------|-------------|-----------------|
| T-03-03 | T (Tampering) | ordering window logic | mitigate | Load `ordering_config` server-side; ensure all critical actions are disabled/blocked when closed |
| T-03-04 | I (Info Disclosure) | ordering messages | accept | Banner reveals only business status and delivery date (non-sensitive) |
</threat_model>

<verification>
- `npm run build` passes.
- Manual spot check: temporarily set `ordering_config.is_ordering_open=false` in DB and confirm `/checkout` shows blocked state.
</verification>

<success_criteria>
- There is a single server-only accessor for ordering state and `/checkout` is blocked when closed.
</success_criteria>

<output>
After completion, create `.planning/phases/03-interactive-shop/03-02-SUMMARY.md`
</output>

