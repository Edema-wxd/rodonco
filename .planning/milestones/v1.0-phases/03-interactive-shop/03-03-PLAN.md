---
phase: 03-interactive-shop
plan: 03
type: execute
wave: 2
depends_on:
  - 03-01
  - 03-02
files_modified:
  - src/store/cartUi.ts
  - src/components/cart/CartSidebar.tsx
  - src/components/layout/Navbar.tsx
  - src/app/(customer)/layout.tsx
autonomous: true
requirements:
  - CART-03
  - CART-05
  - CART-02
must_haves:
  truths:
    - "Cart sidebar can be opened from anywhere via navbar cart icon (CART-03)"
    - "Cart shows itemised list + subtotal + 'Free delivery on Saturdays' note (CART-05)"
    - "Cart quantity edits are disabled when ordering is closed (D-12) but cart remains viewable"
    - "Cart sidebar auto-opens only on first add-to-cart (D-10) via a persisted 'hasAutoOpened' flag"
  artifacts:
    - path: "src/store/cartUi.ts"
      provides: "UI state store for cart sidebar open/close and first-add auto-open"
    - path: "src/components/cart/CartSidebar.tsx"
      provides: "Sidebar UI rendering cart items and subtotal"
    - path: "src/app/(customer)/layout.tsx"
      provides: "Cart sidebar mounted globally in customer layout"
  key_links:
    - from: "src/components/layout/Navbar.tsx"
      to: "src/store/cartUi.ts"
      via: "openCart() on click"
      pattern: "openCart"
    - from: "src/components/cart/CartSidebar.tsx"
      to: "src/store/cart.ts"
      via: "render items + updateQuantity/removeItem"
      pattern: "updateQuantity|removeItem"
    - from: "src/components/cart/CartSidebar.tsx"
      to: "src/components/shop/OrderingClosedBanner.tsx"
      via: "sticky banner when closed"
      pattern: "OrderingClosedBanner"
---

<objective>
Implement a global cart sidebar that is accessible from anywhere, shows itemised cart + subtotal, and honors ordering-closed view-only behavior. Add a small UI store to satisfy “auto-open only on first add-to-cart” and to let the navbar cart icon open the sidebar.

Purpose: This is the primary cart interaction surface for Phase 3 and must be consistent with the cart store’s compound-key identity and ordering window enforcement decisions.
Output: `CartSidebar` + `useCartUiStore` + navbar/layout wiring.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-interactive-shop/03-CONTEXT.md
@src/store/cart.ts
@src/types/index.ts
@src/components/layout/Navbar.tsx
@src/app/(customer)/layout.tsx
@src/components/shop/OrderingClosedBanner.tsx
@src/lib/shop/orderingConfig.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add cart sidebar UI store (open/close + first-add auto-open)</name>
  <files>src/store/cartUi.ts</files>
  <read_first>
    - Read Phase 3 decisions D-10 and D-12 in `03-CONTEXT.md` to match the UX rules.
  </read_first>
  <action>
    - Create a zustand store `useCartUiStore` with:
      - `isOpen: boolean`
      - `openCart()` / `closeCart()` / `toggleCart()`
      - `hasAutoOpened: boolean` persisted (so “first add” is stable across refresh)
      - `autoOpenOnFirstAdd()` that opens the cart only when `hasAutoOpened=false`, then sets it true.
    - Persist only the minimal boolean(s) needed for correctness (avoid persisting transient UI).
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run build</automated>
  </verify>
  <done>
    - Store can be imported by client components without SSR/hydration issues.
    - `autoOpenOnFirstAdd()` is idempotent after the first invocation.
  </done>
</task>

<task type="auto">
  <name>Task 2: Build cart sidebar UI (item list + subtotal + view-only when closed)</name>
  <files>src/components/cart/CartSidebar.tsx</files>
  <read_first>
    - Read `src/store/cart.ts` to use the updated function signatures (prepOption included).
    - Read `src/types/index.ts` for `CartItem` fields and money conventions.
    - Read `src/components/shop/OrderingClosedBanner.tsx` to reuse the banner per D-13.
  </read_first>
  <action>
    - Create `CartSidebar` as a client component that:
      - Renders as a fixed overlay/drawer on the right for desktop and bottom sheet for mobile (basic responsiveness).
      - Shows sticky `OrderingClosedBanner` when ordering is closed (prop-driven).
      - Lists cart items with product name + variant label + prep option, quantity controls, per-line subtotal.
      - Shows overall cart subtotal and the note: `Free delivery on Saturdays`.
    - Enforce D-12: when ordering is closed, disable quantity increment/decrement and remove controls (view-only cart).
    - Keep money math in integer kobo; format to NGN for display (create a tiny local formatter helper inside the component or reuse an existing formatter if present).
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run build</automated>
  </verify>
  <done>
    - When ordering is open, user can increment/decrement/remove items; updates affect only the matching (product, variantLabel, prepOption) key.
    - When ordering is closed, cart is still visible but quantity/remove controls are disabled.
  </done>
</task>

<task type="auto">
  <name>Task 3: Wire navbar cart icon to open cart sidebar and mount globally</name>
  <files>
src/components/layout/Navbar.tsx
src/app/(customer)/layout.tsx
  </files>
  <read_first>
    - Read `Navbar.tsx` to change cart icon from link-to-checkout into “open cart sidebar” per CART-03.
    - Read customer layout to mount cart sidebar outside route-specific trees.
  </read_first>
  <action>
    - Update `Navbar` cart icon behavior:
      - Clicking the cart icon opens the cart sidebar (does not navigate to `/checkout`).
      - Keep the badge logic (hydration-safe) intact.
    - Mount `CartSidebar` in `src/app/(customer)/layout.tsx` so it exists on all customer pages.
    - `CartSidebar` should receive ordering-open state:
      - Add a server boundary in layout that calls `getOrderingConfig()` and passes `is_ordering_open` down as a prop (avoid importing server-only modules into client components).
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run build</automated>
  </verify>
  <done>
    - Cart sidebar can be opened from any customer route.
    - Navbar cart icon no longer navigates away when clicked.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server ordering config → client cart UI | Ordering-closed rules must be enforced consistently in interactive controls |
| client UI actions → cart store | User can spam clicks; store transitions must remain deterministic |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|----------|----------|-----------|-------------|-----------------|
| T-03-05 | T (Tampering) | cart edits when closed | mitigate | Disable quantity/remove controls when `is_ordering_open=false` (D-12) |
| T-03-06 | D (DoS) | sidebar open state | accept | UI-only; keep state minimal and persisted only for first-add rule |
</threat_model>

<verification>
- `npm run build` passes.
- Manual smoke:
  - Open any page → click cart icon → sidebar opens.
  - With ordering closed in DB → sidebar still opens but controls are disabled.
</verification>

<success_criteria>
- Cart sidebar exists globally, is openable via navbar, and honors view-only rules when ordering is closed.
</success_criteria>

<output>
After completion, create `.planning/phases/03-interactive-shop/03-03-SUMMARY.md`
</output>

