---
phase: 07-missing-pages-route-completeness
plan: 02
type: execute
wave: 2
depends_on:
  - 07-01
files_modified:
  - src/app/(customer)/privacy/page.tsx
  - src/app/(customer)/terms/page.tsx
autonomous: true
requirements:
  - R2
  - R3

must_haves:
  truths:
    - "/privacy resolves to a real on-brand page (not a 404)"
    - "/terms resolves to a real on-brand page (not a 404)"
    - "Both pages have bg-stone-100 as root background class"
    - "Both pages use Quicksand for H1 via style={{ fontFamily: 'var(--font-quicksand)' }}"
    - "Both pages export metadata with correct page titles"
    - "npx tsc --noEmit exits 0 after both files are written"
  artifacts:
    - path: "src/app/(customer)/privacy/page.tsx"
      provides: "Privacy Policy static page inheriting (customer) layout (Navbar + Footer auto-included)"
      contains: "bg-stone-100"
    - path: "src/app/(customer)/terms/page.tsx"
      provides: "Terms of Service static page inheriting (customer) layout"
      contains: "bg-stone-100"
  key_links:
    - from: "src/app/(customer)/layout.tsx"
      to: "src/app/(customer)/privacy/page.tsx"
      via: "route group layout inheritance (automatic)"
      pattern: "privacy"
    - from: "src/app/(customer)/layout.tsx"
      to: "src/app/(customer)/terms/page.tsx"
      via: "route group layout inheritance (automatic)"
      pattern: "terms"
---

<objective>
Create the Privacy Policy and Terms of Service static pages inside the (customer) route group.

Purpose: The Footer Legal column links to /privacy and /terms. Both are dead links until this plan ships. After this plan, clicking either Footer link reaches an on-brand, SEO-titled page.

Output:
- `src/app/(customer)/privacy/page.tsx` — Privacy Policy page
- `src/app/(customer)/terms/page.tsx` — Terms of Service page

Both pages inherit Navbar and Footer automatically from `src/app/(customer)/layout.tsx`. No manual Navbar/Footer import is needed. Both pages are server components (no "use client"). Legal copy is placeholder text — it is a client deliverable, not a code deliverable.
</objective>

<execution_context>
@/Users/mac/Documents/GitHub/rodonco/.planning/phases/07-missing-pages-route-completeness/07-RESEARCH.md
@/Users/mac/Documents/GitHub/rodonco/.planning/phases/07-missing-pages-route-completeness/07-PATTERNS.md
</execution_context>

<context>
@/Users/mac/Documents/GitHub/rodonco/.planning/ROADMAP.md

<interfaces>
<!-- Key contracts from (customer) layout and how-it-works template -->

From src/app/(customer)/layout.tsx — layout wraps children with Navbar, Footer, CartSidebar:
DO NOT import Navbar or Footer in the new pages — they are auto-inherited.

From src/app/(customer)/how-it-works/page.tsx — authoritative design system template:
```tsx
import Link from "next/link";

export default function PageName() {
  return (
    <div className="bg-stone-100">
      <section className="overflow-hidden bg-stone-100 py-16 sm:py-20">
        <div className="mx-auto max-w-7xl px-8">
          {/* back link — Lexend, text-red-700 */}
          <div className="mb-8">
            <Link href="/" className="text-sm font-black uppercase tracking-wider text-red-700"
              style={{ fontFamily: "var(--font-lexend)" }}>
              ← Back to home
            </Link>
          </div>
          {/* section label — Lexend, text-red-600, text-xs */}
          <p className="text-xs font-black uppercase tracking-wider text-red-600"
            style={{ fontFamily: "var(--font-lexend)" }}>
            Legal
          </p>
          {/* H1 — Quicksand per ROADMAP spec */}
          <h1 className="mt-4 text-5xl font-black leading-[1.05] text-zinc-800"
            style={{ fontFamily: "var(--font-quicksand)" }}>
            Page Title
          </h1>
          {/* body — Inter */}
          <p className="mt-6 text-base leading-7 text-stone-600"
            style={{ fontFamily: "var(--font-inter)" }}>
            {/* content */}
          </p>
        </div>
      </section>
    </div>
  );
}
```

Content card pattern (for legal sections):
```tsx
<div className="relative rounded-tl-[48px] rounded-tr-2xl rounded-bl-2xl rounded-br-[48px] bg-white p-10 shadow-sm outline outline-1 outline-stone-200/60">
  <h3 className="text-2xl font-bold text-zinc-800" style={{ fontFamily: "var(--font-lexend)" }}>
    Section title
  </h3>
  <p className="mt-4 text-base leading-6 text-stone-600" style={{ fontFamily: "var(--font-inter)" }}>
    Body copy.
  </p>
</div>
```

Metadata export pattern (Next.js App Router):
```tsx
import type { Metadata } from "next";
export const metadata: Metadata = { title: "Page Title | Rodo & Co" };
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Privacy Policy page (R2)</name>
  <files>src/app/(customer)/privacy/page.tsx</files>
  <read_first>
    - src/app/(customer)/how-it-works/page.tsx (full file — confirm outer wrapper, section classes, back link, H1 inline style pattern before writing)
    - src/app/(customer)/layout.tsx (confirm Navbar/Footer are auto-injected — verify no manual import needed)
  </read_first>
  <action>
Create the directory `src/app/(customer)/privacy/` and the file `src/app/(customer)/privacy/page.tsx`.

DO NOT add `"use client"` — this is a server component.
DO NOT import Navbar or Footer — they are auto-injected by the (customer) layout.

Write the file with exactly this content:

```tsx
import type { Metadata } from "next";
import Link from "next/link";

export const metadata: Metadata = {
  title: "Privacy Policy | Rodo & Co",
};

export default function PrivacyPage() {
  return (
    <div className="bg-stone-100">
      {/* Hero */}
      <section className="overflow-hidden bg-stone-100 py-16 sm:py-20">
        <div className="mx-auto max-w-7xl px-8">
          {/* Back link */}
          <div className="mb-8">
            <Link
              href="/"
              className="text-sm font-black uppercase tracking-wider text-red-700"
              style={{ fontFamily: "var(--font-lexend)" }}
            >
              ← Back to home
            </Link>
          </div>

          {/* Section label */}
          <p
            className="text-xs font-black uppercase tracking-wider text-red-600"
            style={{ fontFamily: "var(--font-lexend)" }}
          >
            Legal
          </p>

          {/* H1 */}
          <h1
            className="mt-4 text-5xl font-black leading-[1.05] text-zinc-800"
            style={{ fontFamily: "var(--font-quicksand)" }}
          >
            Privacy Policy
          </h1>

          {/* Intro */}
          <p
            className="mt-6 max-w-2xl text-base leading-7 text-stone-600"
            style={{ fontFamily: "var(--font-inter)" }}
          >
            {/* PLACEHOLDER: Legal copy is a client deliverable. Content below is a structural placeholder only. */}
            This Privacy Policy describes how Rodo &amp; Co collects, uses, and protects your
            personal information in accordance with the Nigeria Data Protection Regulation (NDPR).
          </p>
        </div>
      </section>

      {/* Content */}
      <section className="overflow-hidden bg-stone-100 pb-24 sm:pb-28">
        <div className="mx-auto max-w-7xl px-8">
          <div className="grid grid-cols-1 gap-6 md:grid-cols-2">
            {/* Card 1 */}
            <div className="relative rounded-tl-[48px] rounded-tr-2xl rounded-bl-2xl rounded-br-[48px] bg-white p-10 shadow-sm outline outline-1 outline-stone-200/60">
              <h3
                className="text-2xl font-bold text-zinc-800"
                style={{ fontFamily: "var(--font-lexend)" }}
              >
                Information We Collect
              </h3>
              <p
                className="mt-4 text-base leading-6 text-stone-600"
                style={{ fontFamily: "var(--font-inter)" }}
              >
                {/* PLACEHOLDER — client to supply */}
                We collect your name, phone number, email address, and delivery address when you
                place an order. No payment card details are stored on our servers.
              </p>
            </div>

            {/* Card 2 */}
            <div className="relative rounded-tl-[48px] rounded-tr-2xl rounded-bl-2xl rounded-br-[48px] bg-white p-10 shadow-sm outline outline-1 outline-stone-200/60">
              <h3
                className="text-2xl font-bold text-zinc-800"
                style={{ fontFamily: "var(--font-lexend)" }}
              >
                How We Use Your Data
              </h3>
              <p
                className="mt-4 text-base leading-6 text-stone-600"
                style={{ fontFamily: "var(--font-inter)" }}
              >
                {/* PLACEHOLDER — client to supply */}
                Your data is used solely to process and fulfil your order and to send delivery
                confirmations. We do not sell or share your personal data with third parties.
              </p>
            </div>

            {/* Card 3 */}
            <div className="relative rounded-tl-[48px] rounded-tr-2xl rounded-bl-2xl rounded-br-[48px] bg-white p-10 shadow-sm outline outline-1 outline-stone-200/60">
              <h3
                className="text-2xl font-bold text-zinc-800"
                style={{ fontFamily: "var(--font-lexend)" }}
              >
                Your Rights
              </h3>
              <p
                className="mt-4 text-base leading-6 text-stone-600"
                style={{ fontFamily: "var(--font-inter)" }}
              >
                {/* PLACEHOLDER — client to supply */}
                Under the NDPR, you have the right to access, correct, or request deletion of your
                personal data. Contact us at hello@rodoco.ng to exercise these rights.
              </p>
            </div>

            {/* Card 4 */}
            <div className="relative rounded-tl-[48px] rounded-tr-2xl rounded-bl-2xl rounded-br-[48px] bg-white p-10 shadow-sm outline outline-1 outline-stone-200/60">
              <h3
                className="text-2xl font-bold text-zinc-800"
                style={{ fontFamily: "var(--font-lexend)" }}
              >
                Cookies
              </h3>
              <p
                className="mt-4 text-base leading-6 text-stone-600"
                style={{ fontFamily: "var(--font-inter)" }}
              >
                {/* PLACEHOLDER — client to supply */}
                We use essential cookies to maintain your shopping session. See our{" "}
                <Link href="/cookie-policy" className="underline text-red-700">
                  Cookie Policy
                </Link>{" "}
                for details.
              </p>
            </div>
          </div>

          <div className="mt-14 flex justify-center">
            <Link
              href="/"
              className="border-b-2 border-red-700 pb-0.5 text-sm font-black uppercase tracking-wider text-red-700"
              style={{ fontFamily: "var(--font-lexend)" }}
            >
              Back to home
            </Link>
          </div>
        </div>
      </section>
    </div>
  );
}
```
  </action>
  <acceptance_criteria>
    - `test -f src/app/(customer)/privacy/page.tsx` — file exists
    - `grep -c '"use client"' src/app/(customer)/privacy/page.tsx` outputs `0`
    - `grep -c 'import.*Navbar' src/app/(customer)/privacy/page.tsx` outputs `0`
    - `grep -c 'import.*Footer' src/app/(customer)/privacy/page.tsx` outputs `0`
    - `grep -c 'bg-stone-100' src/app/(customer)/privacy/page.tsx` outputs at least `1`
    - `grep -c 'var(--font-quicksand)' src/app/(customer)/privacy/page.tsx` outputs `1`
    - `grep -c 'Privacy Policy | Rodo' src/app/(customer)/privacy/page.tsx` outputs `1`
    - `grep -c 'outline-stone-200/60' src/app/(customer)/privacy/page.tsx` outputs at least `1`
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/mac/Documents/GitHub/rodonco && test -f src/app/\(customer\)/privacy/page.tsx && grep -c 'bg-stone-100' src/app/\(customer\)/privacy/page.tsx && grep -c 'var(--font-quicksand)' src/app/\(customer\)/privacy/page.tsx && grep -c '"use client"' src/app/\(customer\)/privacy/page.tsx && npx tsc --noEmit</automated>
  </verify>
  <done>src/app/(customer)/privacy/page.tsx exists. No "use client", no Navbar/Footer imports. bg-stone-100 root background, Quicksand H1 via inline style, metadata title "Privacy Policy | Rodo & Co". tsc --noEmit exits 0.</done>
</task>

<task type="auto">
  <name>Task 2: Terms of Service page (R3)</name>
  <files>src/app/(customer)/terms/page.tsx</files>
  <read_first>
    - src/app/(customer)/privacy/page.tsx (the file just created — use as direct structural template; only change title, H1, and card content)
  </read_first>
  <action>
Create the directory `src/app/(customer)/terms/` and the file `src/app/(customer)/terms/page.tsx`.

This page is structurally identical to privacy/page.tsx. Copy the same structure, changing:
- `metadata.title` → `"Terms of Service | Rodo & Co"`
- H1 text → `"Terms of Service"`
- Section label stays `"Legal"`
- Intro paragraph → Terms-appropriate placeholder
- Card headings + body text → Terms-appropriate placeholder content

Write the file with exactly this content:

```tsx
import type { Metadata } from "next";
import Link from "next/link";

export const metadata: Metadata = {
  title: "Terms of Service | Rodo & Co",
};

export default function TermsPage() {
  return (
    <div className="bg-stone-100">
      {/* Hero */}
      <section className="overflow-hidden bg-stone-100 py-16 sm:py-20">
        <div className="mx-auto max-w-7xl px-8">
          {/* Back link */}
          <div className="mb-8">
            <Link
              href="/"
              className="text-sm font-black uppercase tracking-wider text-red-700"
              style={{ fontFamily: "var(--font-lexend)" }}
            >
              ← Back to home
            </Link>
          </div>

          {/* Section label */}
          <p
            className="text-xs font-black uppercase tracking-wider text-red-600"
            style={{ fontFamily: "var(--font-lexend)" }}
          >
            Legal
          </p>

          {/* H1 */}
          <h1
            className="mt-4 text-5xl font-black leading-[1.05] text-zinc-800"
            style={{ fontFamily: "var(--font-quicksand)" }}
          >
            Terms of Service
          </h1>

          {/* Intro */}
          <p
            className="mt-6 max-w-2xl text-base leading-7 text-stone-600"
            style={{ fontFamily: "var(--font-inter)" }}
          >
            {/* PLACEHOLDER: Legal copy is a client deliverable. Content below is a structural placeholder only. */}
            By placing an order with Rodo &amp; Co you agree to these Terms of Service. Please
            read them carefully before completing your purchase.
          </p>
        </div>
      </section>

      {/* Content */}
      <section className="overflow-hidden bg-stone-100 pb-24 sm:pb-28">
        <div className="mx-auto max-w-7xl px-8">
          <div className="grid grid-cols-1 gap-6 md:grid-cols-2">
            {/* Card 1 */}
            <div className="relative rounded-tl-[48px] rounded-tr-2xl rounded-bl-2xl rounded-br-[48px] bg-white p-10 shadow-sm outline outline-1 outline-stone-200/60">
              <h3
                className="text-2xl font-bold text-zinc-800"
                style={{ fontFamily: "var(--font-lexend)" }}
              >
                Orders &amp; Payments
              </h3>
              <p
                className="mt-4 text-base leading-6 text-stone-600"
                style={{ fontFamily: "var(--font-inter)" }}
              >
                {/* PLACEHOLDER — client to supply */}
                Orders are placed for delivery on the following Saturday. Payment is processed
                securely via Paystack. All prices are in Nigerian Naira (₦).
              </p>
            </div>

            {/* Card 2 */}
            <div className="relative rounded-tl-[48px] rounded-tr-2xl rounded-bl-2xl rounded-br-[48px] bg-white p-10 shadow-sm outline outline-1 outline-stone-200/60">
              <h3
                className="text-2xl font-bold text-zinc-800"
                style={{ fontFamily: "var(--font-lexend)" }}
              >
                Cancellations &amp; Refunds
              </h3>
              <p
                className="mt-4 text-base leading-6 text-stone-600"
                style={{ fontFamily: "var(--font-inter)" }}
              >
                {/* PLACEHOLDER — client to supply */}
                Orders may be cancelled before the weekly cutoff on Thursday at midnight. Refund
                policy details to be provided by client.
              </p>
            </div>

            {/* Card 3 */}
            <div className="relative rounded-tl-[48px] rounded-tr-2xl rounded-bl-2xl rounded-br-[48px] bg-white p-10 shadow-sm outline outline-1 outline-stone-200/60">
              <h3
                className="text-2xl font-bold text-zinc-800"
                style={{ fontFamily: "var(--font-lexend)" }}
              >
                Delivery
              </h3>
              <p
                className="mt-4 text-base leading-6 text-stone-600"
                style={{ fontFamily: "var(--font-inter)" }}
              >
                {/* PLACEHOLDER — client to supply */}
                Deliveries are made every Saturday. The specific delivery window and service area
                details will be communicated at checkout.
              </p>
            </div>

            {/* Card 4 */}
            <div className="relative rounded-tl-[48px] rounded-tr-2xl rounded-bl-2xl rounded-br-[48px] bg-white p-10 shadow-sm outline outline-1 outline-stone-200/60">
              <h3
                className="text-2xl font-bold text-zinc-800"
                style={{ fontFamily: "var(--font-lexend)" }}
              >
                Contact
              </h3>
              <p
                className="mt-4 text-base leading-6 text-stone-600"
                style={{ fontFamily: "var(--font-inter)" }}
              >
                {/* PLACEHOLDER — client to supply */}
                For questions about these terms, contact us at hello@rodoco.ng. Full legal entity
                details to be supplied by client.
              </p>
            </div>
          </div>

          <div className="mt-14 flex justify-center">
            <Link
              href="/"
              className="border-b-2 border-red-700 pb-0.5 text-sm font-black uppercase tracking-wider text-red-700"
              style={{ fontFamily: "var(--font-lexend)" }}
            >
              Back to home
            </Link>
          </div>
        </div>
      </section>
    </div>
  );
}
```
  </action>
  <acceptance_criteria>
    - `test -f src/app/(customer)/terms/page.tsx` — file exists
    - `grep -c '"use client"' src/app/(customer)/terms/page.tsx` outputs `0`
    - `grep -c 'import.*Navbar' src/app/(customer)/terms/page.tsx` outputs `0`
    - `grep -c 'import.*Footer' src/app/(customer)/terms/page.tsx` outputs `0`
    - `grep -c 'bg-stone-100' src/app/(customer)/terms/page.tsx` outputs at least `1`
    - `grep -c 'var(--font-quicksand)' src/app/(customer)/terms/page.tsx` outputs `1`
    - `grep -c 'Terms of Service | Rodo' src/app/(customer)/terms/page.tsx` outputs `1`
    - `grep -c 'outline-stone-200/60' src/app/(customer)/terms/page.tsx` outputs at least `1`
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/mac/Documents/GitHub/rodonco && test -f src/app/\(customer\)/terms/page.tsx && grep -c 'bg-stone-100' src/app/\(customer\)/terms/page.tsx && grep -c 'var(--font-quicksand)' src/app/\(customer\)/terms/page.tsx && grep -c '"use client"' src/app/\(customer\)/terms/page.tsx && npx tsc --noEmit</automated>
  </verify>
  <done>src/app/(customer)/terms/page.tsx exists. No "use client", no Navbar/Footer imports. bg-stone-100 root background, Quicksand H1 via inline style, metadata title "Terms of Service | Rodo & Co". tsc --noEmit exits 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → legal pages | Public visitors access static HTML; no user input, no authentication |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-02-01 | Information Disclosure | privacy/page.tsx, terms/page.tsx | accept | Pages are static server components with no DB queries, no user input, no internal path exposure. Placeholder copy does not reveal system internals. Existing CSP headers in next.config.ts apply. |

No high-severity threats. Both pages are static public content. No new ASVS L1 controls required.
</threat_model>

<verification>
After plan 07-02 completes:

```bash
# File existence
test -f src/app/(customer)/privacy/page.tsx    # passes
test -f src/app/(customer)/terms/page.tsx      # passes

# No client directive
grep -c '"use client"' src/app/(customer)/privacy/page.tsx  # → 0
grep -c '"use client"' src/app/(customer)/terms/page.tsx    # → 0

# Design system present
grep -c 'var(--font-quicksand)' src/app/(customer)/privacy/page.tsx  # → 1
grep -c 'var(--font-quicksand)' src/app/(customer)/terms/page.tsx    # → 1

# Type check
npx tsc --noEmit   # → exit 0
```
</verification>

<success_criteria>
- `src/app/(customer)/privacy/page.tsx` exists, server component, exports metadata with title "Privacy Policy | Rodo & Co", H1 "Privacy Policy" in Quicksand
- `src/app/(customer)/terms/page.tsx` exists, server component, exports metadata with title "Terms of Service | Rodo & Co", H1 "Terms of Service" in Quicksand
- Both pages: bg-stone-100 root, Lexend labels, Inter body, asymmetric card corners (outline-stone-200/60)
- `npx tsc --noEmit` exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/07-missing-pages-route-completeness/07-02-SUMMARY.md`
</output>
