---
phase: 07-missing-pages-route-completeness
plan: 03
type: execute
wave: 2
depends_on:
  - 07-01
files_modified:
  - src/app/(customer)/cookie-policy/page.tsx
  - src/app/(customer)/plans/page.tsx
  - src/components/layout/Navbar.tsx
autonomous: true
requirements:
  - R4
  - R5

must_haves:
  truths:
    - "/cookie-policy resolves to a real on-brand page (not a 404)"
    - "/plans resolves to a real on-brand page (not a 404)"
    - "Navbar Plans link href is /plans (not /shop)"
    - "Both new pages have bg-stone-100 as root background class"
    - "Both new pages use Quicksand for H1 via style={{ fontFamily: 'var(--font-quicksand)' }}"
    - "npx tsc --noEmit exits 0 after all three files are written"
  artifacts:
    - path: "src/app/(customer)/cookie-policy/page.tsx"
      provides: "Cookie Policy static page inheriting (customer) layout"
      contains: "bg-stone-100"
    - path: "src/app/(customer)/plans/page.tsx"
      provides: "Plans marketing page — weekly ordering model overview"
      contains: "bg-stone-100"
    - path: "src/components/layout/Navbar.tsx"
      provides: "Navbar with Plans href fixed to /plans"
      contains: '"/plans"'
  key_links:
    - from: "src/components/layout/Navbar.tsx"
      to: "src/app/(customer)/plans/page.tsx"
      via: "navLinks href"
      pattern: 'href.*"/plans"'
    - from: "src/components/landing/Footer.tsx"
      to: "src/app/(customer)/cookie-policy/page.tsx"
      via: "footer Legal column href"
      pattern: "/cookie-policy"
---

<objective>
Create the Cookie Policy and Plans pages, and fix the Navbar Plans href from /shop to /plans.

Purpose: After Wave 0 (07-01), the Footer Legal column has a /cookie-policy link and the Navbar has a Plans link — both are still dead. This plan makes them live. The Plans page also replaces the incorrect /shop placeholder in the Navbar.

Output:
- `src/app/(customer)/cookie-policy/page.tsx` — Cookie Policy static page (same structure as Privacy/Terms)
- `src/app/(customer)/plans/page.tsx` — Plans marketing page describing the weekly ordering model with placeholder pricing content
- `src/components/layout/Navbar.tsx` — Plans navLink href changed from "/shop" to "/plans" (one string, nothing else)

All three changes are independent of each other and can be implemented in any order.
</objective>

<execution_context>
@/Users/mac/Documents/GitHub/rodonco/.planning/phases/07-missing-pages-route-completeness/07-RESEARCH.md
@/Users/mac/Documents/GitHub/rodonco/.planning/phases/07-missing-pages-route-completeness/07-PATTERNS.md
</execution_context>

<context>
@/Users/mac/Documents/GitHub/rodonco/.planning/ROADMAP.md

<interfaces>
<!-- Confirmed interfaces — do not re-read these files to re-confirm, just use the values below -->

From src/components/layout/Navbar.tsx — current navLinks (lines 10–15):
```tsx
const navLinks = [
  { label: "Menu", href: "/shop" },
  { label: "Plans", href: "/shop" },   // ← BUG: must become "/plans"
  { label: "How it Works", href: "/how-it-works" },
  { label: "Wall of Love", href: "/wall-of-love" },
] as const;
```
Change ONLY the Plans href string. Do not restructure the array, change other entries, or add/remove any property.

From src/app/(customer)/how-it-works/page.tsx — authoritative design system template (confirmed):
- Outer wrapper: `<div className="bg-stone-100">`
- Section: `<section className="overflow-hidden bg-stone-100 py-16 sm:py-20">`
- Container: `<div className="mx-auto max-w-7xl px-8">`
- Section label: `className="text-xs font-black uppercase tracking-wider text-red-600"` + `style={{ fontFamily: "var(--font-lexend)" }}`
- H1: `className="mt-4 text-5xl font-black leading-[1.05] text-zinc-800"` + `style={{ fontFamily: "var(--font-quicksand)" }}`
- Body: `className="mt-6 text-base leading-7 text-stone-600"` + `style={{ fontFamily: "var(--font-inter)" }}`
- Card: `className="relative rounded-tl-[48px] rounded-tr-2xl rounded-bl-2xl rounded-br-[48px] bg-white p-10 shadow-sm outline outline-1 outline-stone-200/60"`
- Primary CTA: `className="relative inline-flex items-center justify-center rounded-full bg-red-600 px-10 py-5 shadow-[0px_20px_25px_-5px_rgba(236,45,1,0.30)] transition-opacity hover:opacity-90"`
- Secondary CTA: `className="inline-flex items-center justify-center rounded-full bg-stone-200 px-10 py-5 transition-colors hover:bg-stone-300"`
- Hero badge (green pill): `className="inline-flex items-center rounded-full bg-green-300 px-4 py-1.5 text-xs font-bold uppercase tracking-wider text-green-900"`

Metadata pattern (Next.js App Router):
```tsx
import type { Metadata } from "next";
export const metadata: Metadata = { title: "Page Title | Rodo & Co" };
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Cookie Policy page (R4)</name>
  <files>src/app/(customer)/cookie-policy/page.tsx</files>
  <read_first>
    - src/app/(customer)/privacy/page.tsx (direct structural template — same layout pattern; only change title, H1, section label text, and card content)
  </read_first>
  <action>
Create the directory `src/app/(customer)/cookie-policy/` and the file `src/app/(customer)/cookie-policy/page.tsx`.

DO NOT add `"use client"` — server component.
DO NOT import Navbar or Footer — auto-injected by (customer) layout.

Write the file with exactly this content:

```tsx
import type { Metadata } from "next";
import Link from "next/link";

export const metadata: Metadata = {
  title: "Cookie Policy | Rodo & Co",
};

export default function CookiePolicyPage() {
  return (
    <div className="bg-stone-100">
      {/* Hero */}
      <section className="overflow-hidden bg-stone-100 py-16 sm:py-20">
        <div className="mx-auto max-w-7xl px-8">
          {/* Back link */}
          <div className="mb-8">
            <Link
              href="/"
              className="text-sm font-black uppercase tracking-wider text-red-700"
              style={{ fontFamily: "var(--font-lexend)" }}
            >
              ← Back to home
            </Link>
          </div>

          {/* Section label */}
          <p
            className="text-xs font-black uppercase tracking-wider text-red-600"
            style={{ fontFamily: "var(--font-lexend)" }}
          >
            Legal
          </p>

          {/* H1 */}
          <h1
            className="mt-4 text-5xl font-black leading-[1.05] text-zinc-800"
            style={{ fontFamily: "var(--font-quicksand)" }}
          >
            Cookie Policy
          </h1>

          {/* Intro */}
          <p
            className="mt-6 max-w-2xl text-base leading-7 text-stone-600"
            style={{ fontFamily: "var(--font-inter)" }}
          >
            {/* PLACEHOLDER: Legal copy is a client deliverable. Content below is a structural placeholder only. */}
            This Cookie Policy explains what cookies Rodo &amp; Co uses and why. By continuing to
            use our site, you consent to the use of cookies described here.
          </p>
        </div>
      </section>

      {/* Content */}
      <section className="overflow-hidden bg-stone-100 pb-24 sm:pb-28">
        <div className="mx-auto max-w-7xl px-8">
          <div className="grid grid-cols-1 gap-6 md:grid-cols-2">
            {/* Card 1 */}
            <div className="relative rounded-tl-[48px] rounded-tr-2xl rounded-bl-2xl rounded-br-[48px] bg-white p-10 shadow-sm outline outline-1 outline-stone-200/60">
              <h3
                className="text-2xl font-bold text-zinc-800"
                style={{ fontFamily: "var(--font-lexend)" }}
              >
                What Are Cookies?
              </h3>
              <p
                className="mt-4 text-base leading-6 text-stone-600"
                style={{ fontFamily: "var(--font-inter)" }}
              >
                {/* PLACEHOLDER — client to supply */}
                Cookies are small text files stored on your device when you visit a website. They
                help us remember your preferences and maintain your shopping session.
              </p>
            </div>

            {/* Card 2 */}
            <div className="relative rounded-tl-[48px] rounded-tr-2xl rounded-bl-2xl rounded-br-[48px] bg-white p-10 shadow-sm outline outline-1 outline-stone-200/60">
              <h3
                className="text-2xl font-bold text-zinc-800"
                style={{ fontFamily: "var(--font-lexend)" }}
              >
                Cookies We Use
              </h3>
              <p
                className="mt-4 text-base leading-6 text-stone-600"
                style={{ fontFamily: "var(--font-inter)" }}
              >
                {/* PLACEHOLDER — client to supply */}
                We use essential session cookies to maintain your cart and authentication state.
                We do not use advertising or tracking cookies.
              </p>
            </div>

            {/* Card 3 */}
            <div className="relative rounded-tl-[48px] rounded-tr-2xl rounded-bl-2xl rounded-br-[48px] bg-white p-10 shadow-sm outline outline-1 outline-stone-200/60">
              <h3
                className="text-2xl font-bold text-zinc-800"
                style={{ fontFamily: "var(--font-lexend)" }}
              >
                Managing Cookies
              </h3>
              <p
                className="mt-4 text-base leading-6 text-stone-600"
                style={{ fontFamily: "var(--font-inter)" }}
              >
                {/* PLACEHOLDER — client to supply */}
                You can disable cookies in your browser settings. Note that disabling essential
                cookies may affect your ability to add items to your cart or complete an order.
              </p>
            </div>

            {/* Card 4 */}
            <div className="relative rounded-tl-[48px] rounded-tr-2xl rounded-bl-2xl rounded-br-[48px] bg-white p-10 shadow-sm outline outline-1 outline-stone-200/60">
              <h3
                className="text-2xl font-bold text-zinc-800"
                style={{ fontFamily: "var(--font-lexend)" }}
              >
                Contact
              </h3>
              <p
                className="mt-4 text-base leading-6 text-stone-600"
                style={{ fontFamily: "var(--font-inter)" }}
              >
                {/* PLACEHOLDER — client to supply */}
                If you have questions about our use of cookies, email us at hello@rodoco.ng. Full
                details to be confirmed by client.
              </p>
            </div>
          </div>

          <div className="mt-14 flex justify-center">
            <Link
              href="/"
              className="border-b-2 border-red-700 pb-0.5 text-sm font-black uppercase tracking-wider text-red-700"
              style={{ fontFamily: "var(--font-lexend)" }}
            >
              Back to home
            </Link>
          </div>
        </div>
      </section>
    </div>
  );
}
```
  </action>
  <acceptance_criteria>
    - `test -f src/app/(customer)/cookie-policy/page.tsx` — file exists
    - `grep -c '"use client"' src/app/(customer)/cookie-policy/page.tsx` outputs `0`
    - `grep -c 'import.*Navbar' src/app/(customer)/cookie-policy/page.tsx` outputs `0`
    - `grep -c 'bg-stone-100' src/app/(customer)/cookie-policy/page.tsx` outputs at least `1`
    - `grep -c 'var(--font-quicksand)' src/app/(customer)/cookie-policy/page.tsx` outputs `1`
    - `grep -c 'Cookie Policy | Rodo' src/app/(customer)/cookie-policy/page.tsx` outputs `1`
    - `grep -c 'outline-stone-200/60' src/app/(customer)/cookie-policy/page.tsx` outputs at least `1`
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/mac/Documents/GitHub/rodonco && test -f src/app/\(customer\)/cookie-policy/page.tsx && grep -c 'bg-stone-100' src/app/\(customer\)/cookie-policy/page.tsx && grep -c 'var(--font-quicksand)' src/app/\(customer\)/cookie-policy/page.tsx && npx tsc --noEmit</automated>
  </verify>
  <done>src/app/(customer)/cookie-policy/page.tsx exists. No "use client", no Navbar/Footer imports. bg-stone-100 root, Quicksand H1, metadata title "Cookie Policy | Rodo & Co". tsc --noEmit exits 0.</done>
</task>

<task type="auto">
  <name>Task 2: Plans marketing page (R5)</name>
  <files>src/app/(customer)/plans/page.tsx</files>
  <read_first>
    - src/app/(customer)/how-it-works/page.tsx (authoritative template — confirm hero section structure with centered max-w-3xl block, green badge pattern, CTA button classes)
  </read_first>
  <action>
Create the directory `src/app/(customer)/plans/` and the file `src/app/(customer)/plans/page.tsx`.

DO NOT add `"use client"` — server component.
DO NOT import Navbar or Footer — auto-injected by (customer) layout.

This is a marketing page explaining the weekly ordering model with placeholder pricing. Mark content clearly as "placeholder — pending client copy." Use the same hero structure as how-it-works/page.tsx (centered max-w-3xl text block, green badge, Quicksand H1, CTA buttons) followed by content cards for plan tiers.

Write the file with exactly this content:

```tsx
import type { Metadata } from "next";
import Link from "next/link";

export const metadata: Metadata = {
  title: "Plans | Rodo & Co",
};

export default function PlansPage() {
  return (
    <div className="bg-stone-100">
      {/* Hero */}
      <section className="overflow-hidden bg-stone-100 py-16 sm:py-20">
        <div className="mx-auto max-w-7xl px-8">
          <div className="mx-auto max-w-3xl text-center">
            {/* Badge */}
            <p
              className="inline-flex items-center rounded-full bg-green-300 px-4 py-1.5 text-xs font-bold uppercase tracking-wider text-green-900"
              style={{ fontFamily: "var(--font-lexend)" }}
            >
              Weekly ordering
            </p>

            {/* H1 */}
            <h1
              className="mt-6 text-5xl font-black text-zinc-800 sm:text-6xl"
              style={{ fontFamily: "var(--font-quicksand)" }}
            >
              How Our Plans Work
            </h1>

            {/* Subheadline */}
            <p
              className="mt-4 text-base leading-6 text-stone-600"
              style={{ fontFamily: "var(--font-inter)" }}
            >
              {/* PLACEHOLDER: Pricing and plan details are a client deliverable. */}
              Order fresh produce and cooking kits for the week — delivered every Saturday.
              Browse the menu, add to your order, and pay at checkout. No subscription required.
            </p>

            {/* CTAs */}
            <div className="mt-10 flex flex-wrap justify-center gap-4">
              <Link
                href="/shop"
                className="relative inline-flex items-center justify-center rounded-full bg-red-600 px-10 py-5 shadow-[0px_20px_25px_-5px_rgba(236,45,1,0.30)] transition-opacity hover:opacity-90"
              >
                <span
                  className="text-lg font-bold text-rose-50"
                  style={{ fontFamily: "var(--font-lexend)" }}
                >
                  Browse the menu
                </span>
              </Link>
              <Link
                href="/"
                className="inline-flex items-center justify-center rounded-full bg-stone-200 px-10 py-5 transition-colors hover:bg-stone-300"
              >
                <span
                  className="text-lg font-bold text-zinc-800"
                  style={{ fontFamily: "var(--font-lexend)" }}
                >
                  Back to home
                </span>
              </Link>
            </div>
          </div>
        </div>
      </section>

      {/* Plan tiers */}
      <section className="overflow-hidden bg-stone-100 pb-24 sm:pb-28">
        <div className="mx-auto max-w-7xl px-8">
          <div className="mb-14">
            <h2
              className="text-4xl font-black uppercase text-zinc-800"
              style={{ fontFamily: "var(--font-lexend)" }}
            >
              Weekly ordering, <span className="text-green-800">simplified.</span>
            </h2>
            <p
              className="mt-4 max-w-2xl text-base leading-6 text-stone-600"
              style={{ fontFamily: "var(--font-inter)" }}
            >
              {/* PLACEHOLDER — client to supply pricing details */}
              Pick what you need each week — no recurring commitment. Ordering opens every
              Sunday and closes Thursday night for Saturday delivery.
            </p>
          </div>

          <div className="grid grid-cols-1 gap-6 md:grid-cols-2">
            {/* Card 1 — Fresh Produce */}
            <div className="relative rounded-tl-[48px] rounded-tr-2xl rounded-bl-2xl rounded-br-[48px] bg-white p-10 shadow-sm outline outline-1 outline-stone-200/60">
              <p
                className="inline-flex items-center rounded-full bg-green-300 px-3 py-1 text-xs font-bold uppercase tracking-wider text-green-900"
                style={{ fontFamily: "var(--font-lexend)" }}
              >
                Fresh Produce
              </p>
              <h3
                className="mt-6 text-2xl font-bold text-zinc-800"
                style={{ fontFamily: "var(--font-lexend)" }}
              >
                Weekly Essentials
              </h3>
              <p
                className="mt-4 text-base leading-6 text-stone-600"
                style={{ fontFamily: "var(--font-inter)" }}
              >
                {/* PLACEHOLDER — client to supply pricing */}
                Choose individual fresh produce items for the week. Select your preferred prep
                option (whole, sliced, or diced) and quantity. Priced per portion.
              </p>
              <p
                className="mt-6 text-3xl font-black text-zinc-800"
                style={{ fontFamily: "var(--font-quicksand)" }}
              >
                From ₦2,500
                <span
                  className="ml-2 text-sm font-normal text-stone-500"
                  style={{ fontFamily: "var(--font-inter)" }}
                >
                  / item {/* PLACEHOLDER */}
                </span>
              </p>
            </div>

            {/* Card 2 — Cooking Kits */}
            <div className="relative rounded-tl-[48px] rounded-tr-2xl rounded-bl-2xl rounded-br-[48px] bg-white p-10 shadow-sm outline outline-1 outline-stone-200/60">
              <p
                className="inline-flex items-center rounded-full bg-red-100 px-3 py-1 text-xs font-bold uppercase tracking-wider text-red-700"
                style={{ fontFamily: "var(--font-lexend)" }}
              >
                Cooking Kits
              </p>
              <h3
                className="mt-6 text-2xl font-bold text-zinc-800"
                style={{ fontFamily: "var(--font-lexend)" }}
              >
                Ready-to-Cook Kits
              </h3>
              <p
                className="mt-4 text-base leading-6 text-stone-600"
                style={{ fontFamily: "var(--font-inter)" }}
              >
                {/* PLACEHOLDER — client to supply pricing */}
                Pre-portioned cooking kits with everything you need for a recipe. Available in
                multiple sizes. Just cook, plate, and enjoy.
              </p>
              <p
                className="mt-6 text-3xl font-black text-zinc-800"
                style={{ fontFamily: "var(--font-quicksand)" }}
              >
                From ₦8,500
                <span
                  className="ml-2 text-sm font-normal text-stone-500"
                  style={{ fontFamily: "var(--font-inter)" }}
                >
                  / kit {/* PLACEHOLDER */}
                </span>
              </p>
            </div>

            {/* Card 3 — How ordering works */}
            <div className="relative rounded-tl-[48px] rounded-tr-2xl rounded-bl-2xl rounded-br-[48px] bg-white p-10 shadow-sm outline outline-1 outline-stone-200/60 md:col-span-2">
              <h3
                className="text-2xl font-bold text-zinc-800"
                style={{ fontFamily: "var(--font-lexend)" }}
              >
                The ordering window
              </h3>
              <p
                className="mt-4 text-base leading-6 text-stone-600"
                style={{ fontFamily: "var(--font-inter)" }}
              >
                {/* PLACEHOLDER — client to supply any additional detail */}
                Ordering opens every Sunday and closes Thursday at midnight. Once closed,
                the menu is still browsable but items cannot be added to your order until the
                next window opens. Delivery is every Saturday — fresh to your door.
              </p>
            </div>
          </div>

          <div className="mt-14 flex justify-center">
            <Link
              href="/shop"
              className="border-b-2 border-red-700 pb-0.5 text-sm font-black uppercase tracking-wider text-red-700"
              style={{ fontFamily: "var(--font-lexend)" }}
            >
              Browse this week&apos;s menu
            </Link>
          </div>
        </div>
      </section>
    </div>
  );
}
```
  </action>
  <acceptance_criteria>
    - `test -f src/app/(customer)/plans/page.tsx` — file exists
    - `grep -c '"use client"' src/app/(customer)/plans/page.tsx` outputs `0`
    - `grep -c 'import.*Navbar' src/app/(customer)/plans/page.tsx` outputs `0`
    - `grep -c 'bg-stone-100' src/app/(customer)/plans/page.tsx` outputs at least `1`
    - `grep -c 'var(--font-quicksand)' src/app/(customer)/plans/page.tsx` outputs at least `1`
    - `grep -c 'Plans | Rodo' src/app/(customer)/plans/page.tsx` outputs `1`
    - `grep -c 'PLACEHOLDER' src/app/(customer)/plans/page.tsx` outputs at least `3`
    - `grep -c 'outline-stone-200/60' src/app/(customer)/plans/page.tsx` outputs at least `1`
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/mac/Documents/GitHub/rodonco && test -f src/app/\(customer\)/plans/page.tsx && grep -c 'bg-stone-100' src/app/\(customer\)/plans/page.tsx && grep -c 'var(--font-quicksand)' src/app/\(customer\)/plans/page.tsx && grep -c '"use client"' src/app/\(customer\)/plans/page.tsx && npx tsc --noEmit</automated>
  </verify>
  <done>src/app/(customer)/plans/page.tsx exists. No "use client", no Navbar/Footer imports. bg-stone-100 root, Quicksand H1 "How Our Plans Work", green badge, CTA buttons, plan tier cards with placeholder pricing, metadata title "Plans | Rodo & Co". tsc --noEmit exits 0.</done>
</task>

<task type="auto">
  <name>Task 3: Navbar Plans href /shop → /plans (R5)</name>
  <files>src/components/layout/Navbar.tsx</files>
  <read_first>
    - src/components/layout/Navbar.tsx (full file — confirm exact current state of navLinks before editing; find the line with label "Plans" and href "/shop")
  </read_first>
  <action>
Edit `src/components/layout/Navbar.tsx`. Make exactly one change:

In the `navLinks` array (lines 10–15), change the `href` value on the Plans entry from `"/shop"` to `"/plans"`.

**Before:**
```tsx
const navLinks = [
  { label: "Menu", href: "/shop" },
  { label: "Plans", href: "/shop" },
  { label: "How it Works", href: "/how-it-works" },
  { label: "Wall of Love", href: "/wall-of-love" },
] as const;
```

**After:**
```tsx
const navLinks = [
  { label: "Menu", href: "/shop" },
  { label: "Plans", href: "/plans" },
  { label: "How it Works", href: "/how-it-works" },
  { label: "Wall of Love", href: "/wall-of-love" },
] as const;
```

That is the complete change. Do NOT:
- Add or remove any array entries
- Change any other href values
- Change any label values
- Restructure the array or its type annotation
- Touch any other part of the file
  </action>
  <acceptance_criteria>
    - `grep -n '"Plans"' src/components/layout/Navbar.tsx` output shows `href: "/plans"` on that line
    - `grep -c 'href: "/plans"' src/components/layout/Navbar.tsx` outputs `1`
    - Verify the Menu entry still points to /shop: `grep -c '{ label: "Menu", href: "/shop" }' src/components/layout/Navbar.tsx` outputs `1`
    - `grep -c '{ label: "Plans", href: "/shop" }' src/components/layout/Navbar.tsx` outputs `0`
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/mac/Documents/GitHub/rodonco && grep -n '"Plans"' src/components/layout/Navbar.tsx && grep -c 'href: "/plans"' src/components/layout/Navbar.tsx && grep -c '{ label: "Plans", href: "/shop" }' src/components/layout/Navbar.tsx && npx tsc --noEmit</automated>
  </verify>
  <done>Navbar.tsx navLinks Plans entry has href "/plans". Menu entry still has href "/shop". No TypeScript errors. tsc --noEmit exits 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → cookie-policy / plans | Public visitors access static HTML; no user input, no authentication |
| Navbar href → /plans route | Internal navigation link; no data flow |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-03-01 | Information Disclosure | cookie-policy/page.tsx, plans/page.tsx | accept | Pages are static server components. No DB queries, no user input, no internal paths exposed. Placeholder pricing copy does not reveal billing system internals. Existing CSP headers apply. |
| T-07-03-02 | Spoofing | Navbar.tsx href change | accept | href changes from /shop to /plans — both are internal routes. No external redirect, no open redirect vector. Change is purely a string value in a static array. |

No high-severity threats. All work is static public content and a single internal href string correction. No new ASVS L1 controls required.
</threat_model>

<verification>
After plan 07-03 completes:

```bash
# File existence
test -f src/app/(customer)/cookie-policy/page.tsx   # passes
test -f src/app/(customer)/plans/page.tsx            # passes

# No client directive
grep -c '"use client"' src/app/(customer)/cookie-policy/page.tsx  # → 0
grep -c '"use client"' src/app/(customer)/plans/page.tsx          # → 0

# Design system
grep -c 'var(--font-quicksand)' src/app/(customer)/cookie-policy/page.tsx  # → 1
grep -c 'var(--font-quicksand)' src/app/(customer)/plans/page.tsx          # → 1

# Navbar fix
grep -c 'href: "/plans"' src/components/layout/Navbar.tsx   # → 1
grep -c '{ label: "Plans", href: "/shop" }' src/components/layout/Navbar.tsx  # → 0

# Full type check
npx tsc --noEmit   # → exit 0

# Full test suite (after both Wave 1 plans complete)
npx vitest run && npx tsc --noEmit   # → all GREEN, exit 0
```
</verification>

<success_criteria>
- `src/app/(customer)/cookie-policy/page.tsx` exists, server component, exports metadata title "Cookie Policy | Rodo & Co", H1 "Cookie Policy" in Quicksand
- `src/app/(customer)/plans/page.tsx` exists, server component, exports metadata title "Plans | Rodo & Co", H1 "How Our Plans Work" in Quicksand, CTA buttons linking to /shop and /, placeholder pricing content marked as PLACEHOLDER
- `src/components/layout/Navbar.tsx` Plans entry href is "/plans" — grep confirms `href: "/plans"` present and `{ label: "Plans", href: "/shop" }` absent
- `npx tsc --noEmit` exits 0
- `npx vitest run` exits 0 (Footer.test.tsx remains green)
</success_criteria>

<output>
After completion, create `.planning/phases/07-missing-pages-route-completeness/07-03-SUMMARY.md`
</output>
