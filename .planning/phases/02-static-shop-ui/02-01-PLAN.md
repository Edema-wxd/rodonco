---
phase: 02-static-shop-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(customer)/page.tsx
  - src/components/landing/HowItWorks.tsx
autonomous: true
requirements:
  - SHOP-01
must_haves:
  truths:
    - "Visitor can open `/` and see a hero section (headline, subheadline, CTA to `/shop`) without client-side JS."
    - "Visitor can see a 3-step How It Works section (Browse → Customise → Deliver) below the hero."
  artifacts:
    - path: "src/app/(customer)/page.tsx"
      provides: "Landing page hero + How It Works sections (server-rendered)"
    - path: "src/components/landing/HowItWorks.tsx"
      provides: "Reusable How It Works section per Phase 2 UI spec"
  key_links:
    - from: "src/app/(customer)/page.tsx"
      to: "/shop"
      via: "next/link CTA"
      pattern: "href=\\\"/shop\\\""
---

<objective>
Implement the Phase 2 landing page UI contract (hero + How It Works) as a server-rendered page.

Purpose: Deliver SHOP-01 with the locked hero and How It Works decisions.
Output: A complete `/` landing page matching `02-CONTEXT.md` + `02-UI-SPEC.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-static-shop-ui/02-CONTEXT.md
@.planning/phases/02-static-shop-ui/02-UI-SPEC.md

@src/app/(customer)/page.tsx
@src/components/layout/Navbar.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build landing hero per locked decisions (D-01..D-03)</name>
  <read_first>
    - .planning/phases/02-static-shop-ui/02-CONTEXT.md (D-01..D-03)
    - .planning/phases/02-static-shop-ui/02-UI-SPEC.md (Landing layout + copy contract)
    - src/app/(customer)/page.tsx (current stub)
  </read_first>
  <files>
    src/app/(customer)/page.tsx
  </files>
  <action>
    Replace the current landing page stub with the Phase 2 hero section:
    - White/light hero (per D-01) with headline + subheadline + single CTA linking to `/shop` (per D-03).
    - CTA uses placeholder brand green `#16a34a` (per D-02) and uses shadcn Button styling conventions (do not introduce a new button component).
    - Match `02-UI-SPEC.md` layout tokens: `py-24` hero vertical padding; typography roles (display headline `text-4xl font-bold`, subheadline `text-base text-muted-foreground`).
    - Keep this as a server component (no `"use client"`).
  </action>
  <acceptance_criteria>
    - `/` renders headline, subheadline, and a single CTA button linking to `/shop` without requiring JS.
    - CTA styling is green `#16a34a` with a darker hover state (per UI-SPEC).
  </acceptance_criteria>
  <verify>
    <automated>npm test</automated>
  </verify>
  <done>
    Landing hero meets SHOP-01 and matches D-01..D-03 + UI-SPEC.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add How It Works section component (D-04..D-06) and render it on `/`</name>
  <read_first>
    - .planning/phases/02-static-shop-ui/02-CONTEXT.md (D-04..D-06)
    - .planning/phases/02-static-shop-ui/02-UI-SPEC.md (How It Works structure + copy)
    - src/app/(customer)/page.tsx (after Task 1)
  </read_first>
  <files>
    src/components/landing/HowItWorks.tsx
    src/app/(customer)/page.tsx
  </files>
  <action>
    Create `src/components/landing/HowItWorks.tsx` as a server component implementing:
    - 3-step flow Browse → Customise → Deliver (per D-04)
    - Each step = Lucide icon + label + short description (per D-05)
    - Section visually distinct from hero using secondary/near-white background (per D-06), with `py-16` spacing per UI-SPEC.
    Render this component below the hero in `src/app/(customer)/page.tsx`.
    Ensure icons are decorative with `aria-hidden="true"` per UI-SPEC accessibility notes.
  </action>
  <acceptance_criteria>
    - `/` contains a "How It Works" section with exactly 3 steps and the specified labels.
    - Section uses a distinct background (`bg-secondary` or equivalent) and spacing consistent with UI-SPEC.
  </acceptance_criteria>
  <verify>
    <automated>npm test</automated>
  </verify>
  <done>
    The landing page fully satisfies SHOP-01 with hero + How It Works rendered server-side.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → Next.js server rendering | No untrusted user input is processed in Phase 2 landing; content is static. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|----------|----------|-----------|-------------|-----------------|
| T-02-01 | T (Tampering) | `src/app/(customer)/page.tsx` links | accept | No dynamic URL construction; CTA href is a constant `/shop`. |
| T-02-02 | I (Information Disclosure) | Landing content | accept | No secrets; no environment variables rendered. |
</threat_model>

<verification>
- `npm test` is green.
- Manual smoke: `npm run dev` then open `/` and confirm hero + How It Works match the Phase 2 UI contract and are visible with JS disabled.
</verification>

<success_criteria>
- `/` renders hero + How It Works per locked decisions D-01..D-06 and satisfies SHOP-01.
- CTA navigates to `/shop`.
</success_criteria>

<output>
After completion, create `.planning/phases/02-static-shop-ui/02-01-SUMMARY.md`
</output>

