---
phase: 02.1
slug: migrate-supabase-to-neon-and-uploadthing
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-04-24
---

# Phase 02.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | None configured — infrastructure-only phase; Wave 0 adds `scripts/smoke-test-db.ts` only |
| **Config file** | none — Wave 0 installs |
| **Quick run command** | `npx drizzle-kit push` (schema validity check) |
| **Full suite command** | `curl -s -o /dev/null -w "%{http_code}" http://localhost:3000/admin/orders` (expects 307) |
| **Estimated runtime** | ~10 seconds |

---

## Sampling Rate

- **After every task commit:** Run `npx drizzle-kit push` (schema still valid)
- **After every plan wave:** Run curl check — `/admin/orders` returns 307 redirect
- **Before `/gsd-verify-work`:** All 7 tables in Neon console + NextAuth sign-in in browser
- **Max feedback latency:** ~30 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 02.1-env-vars | 01 | 1 | D-15/D-16/D-17 | — | `DATABASE_URL` never `NEXT_PUBLIC_` | smoke | `grep -r "NEXT_PUBLIC_DATABASE_URL" src/ \| wc -l` → 0 | ⬜ W0 | ⬜ pending |
| 02.1-drizzle-schema | 01 | 1 | D-01/D-02/D-03 | — | All 7 tables created in Neon | smoke | `npx drizzle-kit push && echo OK` | ✅ exists after W0 | ⬜ pending |
| 02.1-db-client | 01 | 1 | D-04 | — | lib/db/index.ts server-only | smoke | `grep -r "from.*lib/db" src/` (only server files) | ⬜ W0 | ⬜ pending |
| 02.1-nextauth-route | 02 | 2 | D-05/D-06/D-07 | V2/V3 | JWT session signed with AUTH_SECRET | integration | Manual browser sign-in | ⬜ W0 | ⬜ pending |
| 02.1-middleware | 02 | 2 | D-08 | V4 | `/admin/*` redirects unauthenticated | smoke | `curl -I http://localhost:3000/admin/orders` → 307 | ✅ exists | ⬜ pending |
| 02.1-uploadthing | 03 | 3 | D-10/D-11 | V4 | Upload blocked without session | integration | Manual browser upload test | ⬜ W0 | ⬜ pending |
| 02.1-seed-admins | 02 | 2 | D-09 | V2 | Admin row in Neon, password bcrypt-hashed | smoke | `npx tsx scripts/seed-admin.ts && echo OK` | ⬜ W0 | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `scripts/smoke-test-db.ts` — `SELECT 1` against Neon to verify connectivity
- [ ] `scripts/seed-admin.ts` — seed admins table with env-var credentials
- [ ] No test framework — this phase does not install one (infrastructure-only)

*Primary validation is manual: verify all 7 tables in Neon console + NextAuth sign-in + Uploadthing upload in browser*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| NextAuth signs in with valid credentials | D-05/D-06 | No headless browser configured | Visit `/admin`, submit email/password from seed script, verify redirect to `/admin/orders` |
| NextAuth blocks invalid credentials | D-05 | Manual | Submit wrong password, verify error message, no redirect |
| Uploadthing accepts file from authenticated admin | D-11 | No headless browser | Sign in as admin, navigate to product form (Phase 4 UI not built yet — test via API curl with session cookie) |
| All 7 tables exist in Neon with correct columns | D-02/D-03 | Neon console / Drizzle Studio | Open Neon console or run `npx drizzle-kit studio`, verify: products, product_variants, prep_options, orders, order_items, ordering_config, admins |
| `ordering_config` row 1 seeded with `is_ordering_open = true` | D-03 | Drizzle Studio | Verify row exists after `drizzle-kit push` + seed script |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 30s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
