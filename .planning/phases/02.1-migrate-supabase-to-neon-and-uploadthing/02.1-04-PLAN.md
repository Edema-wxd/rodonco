---
phase: 02.1
plan: 04
type: execute
wave: 4
depends_on: ["02.1-01", "02.1-02", "02.1-03"]
files_modified:
  - package.json
  - package-lock.json
  - src/types/env.d.ts
  - .env.local.example
  - src/lib/supabase/admin.ts
  - src/lib/supabase/client.ts
  - src/lib/supabase/server.ts
requirements:
  - D-15
  - D-16
  - D-17
  - D-04
autonomous: true
must_haves:
  truths:
    - "No reference to @supabase/supabase-js or @supabase/ssr remains in package.json or src/"
    - "src/types/env.d.ts declares the new env var set: DATABASE_URL, DIRECT_DATABASE_URL, AUTH_SECRET, UPLOADTHING_TOKEN (and keeps PAYSTACK_*, RESEND_*, ADMIN_NOTIFICATION_EMAIL, CRON_SECRET, NEXT_PUBLIC_PAYSTACK_PUBLIC_KEY, NEXT_PUBLIC_APP_URL)"
    - "src/types/env.d.ts no longer declares NEXT_PUBLIC_SUPABASE_URL, NEXT_PUBLIC_SUPABASE_ANON_KEY, SUPABASE_SERVICE_ROLE_KEY"
    - ".env.local.example matches env.d.ts — same vars, with placeholder values and source comments"
    - "src/lib/supabase/ directory is empty (3 client files removed)"
    - "Production build (npm run build) succeeds"
  artifacts:
    - path: "src/types/env.d.ts"
      provides: "TypeScript ambient declaration for the post-migration env var set"
      contains: "DATABASE_URL"
    - path: ".env.local.example"
      provides: "Documented env var template — what to fill in .env.local"
      contains: "DATABASE_URL"
  key_links:
    - from: "src/types/env.d.ts"
      to: ".env.local.example"
      via: "must list the same vars"
      pattern: "DATABASE_URL"
    - from: "package.json"
      to: "no @supabase/* deps"
      via: "uninstall command"
      pattern: ""
---

<objective>
Final cleanup: uninstall the Supabase npm packages, delete the three `lib/supabase/*.ts` client files, and update `src/types/env.d.ts` + `.env.local.example` to reflect the new env var contract (per D-15, D-16, D-17 — corrected to v5/v7 names per RESEARCH.md).

Purpose: Remove dead code and stale env vars so the migration is clean. After this plan, `grep -r "supabase" src/` should return zero matches and the env contract documents only what the post-migration app actually uses.

Output: Lean dependency tree, empty `src/lib/supabase/` directory, accurate env declarations, accurate `.env.local.example`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02.1-migrate-supabase-to-neon-and-uploadthing/02.1-CONTEXT.md
@.planning/phases/02.1-migrate-supabase-to-neon-and-uploadthing/02.1-RESEARCH.md
@.planning/phases/02.1-migrate-supabase-to-neon-and-uploadthing/02.1-01-PLAN.md
@.planning/phases/02.1-migrate-supabase-to-neon-and-uploadthing/02.1-02-PLAN.md
@.planning/phases/02.1-migrate-supabase-to-neon-and-uploadthing/02.1-03-PLAN.md
@src/types/env.d.ts
@.env.local.example

<interfaces>
<!-- Final env contract — authoritative table from RESEARCH.md "Environment Variables: Final Correct Set" -->

REMOVE from src/types/env.d.ts AND .env.local.example:
- NEXT_PUBLIC_SUPABASE_URL
- NEXT_PUBLIC_SUPABASE_ANON_KEY
- SUPABASE_SERVICE_ROLE_KEY

ADD to src/types/env.d.ts AND .env.local.example:
- DATABASE_URL (server) — Neon pooled URL (runtime queries)
- DIRECT_DATABASE_URL (server) — Neon direct URL (drizzle-kit push only)
- AUTH_SECRET (server) — NextAuth v5 secret, ≥ 32 random bytes (generated by `npx auth secret`)
- UPLOADTHING_TOKEN (server) — Uploadthing v7 token (single base64 value from V7 tab)

OPTIONAL — also acceptable to add:
- AUTH_URL (server, optional) — auto-detected in most envs; set on Vercel for canonical URL

KEEP unchanged in both files:
- NEXT_PUBLIC_PAYSTACK_PUBLIC_KEY
- NEXT_PUBLIC_APP_URL
- PAYSTACK_SECRET_KEY
- PAYSTACK_WEBHOOK_SECRET
- RESEND_API_KEY
- RESEND_FROM_EMAIL
- ADMIN_NOTIFICATION_EMAIL
- CRON_SECRET
- ADMIN_EMAIL (added by Plan 02 seed step — seed-time only but documented for repeatable seeding)
- ADMIN_PASSWORD (added by Plan 02 seed step — seed-time only)

Final src/types/env.d.ts shape:
```typescript
declare namespace NodeJS {
  interface ProcessEnv {
    // ── Public (browser-safe) ──
    NEXT_PUBLIC_PAYSTACK_PUBLIC_KEY: string;
    NEXT_PUBLIC_APP_URL: string;

    // ── Server-only (NEVER prefix with NEXT_PUBLIC_) ──
    DATABASE_URL: string;
    DIRECT_DATABASE_URL: string;
    AUTH_SECRET: string;
    AUTH_URL?: string;
    UPLOADTHING_TOKEN: string;
    PAYSTACK_SECRET_KEY: string;
    PAYSTACK_WEBHOOK_SECRET: string;
    RESEND_API_KEY: string;
    RESEND_FROM_EMAIL: string;
    ADMIN_NOTIFICATION_EMAIL: string;
    ADMIN_EMAIL: string;       // seed-time
    ADMIN_PASSWORD: string;    // seed-time
    CRON_SECRET: string;

    // ── Standard Node ──
    NODE_ENV: "development" | "test" | "production";
  }
}

export {};
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Uninstall @supabase/* packages, delete src/lib/supabase/*.ts files, update env.d.ts and .env.local.example</name>
  <files>package.json, package-lock.json, src/types/env.d.ts, .env.local.example, src/lib/supabase/admin.ts, src/lib/supabase/client.ts, src/lib/supabase/server.ts</files>
  <read_first>
    - .planning/phases/02.1-migrate-supabase-to-neon-and-uploadthing/02.1-RESEARCH.md ("Environment Variables: Final Correct Set" table — authoritative source)
    - .planning/phases/02.1-migrate-supabase-to-neon-and-uploadthing/02.1-CONTEXT.md (D-15, D-16 corrected to v5/v7 names per RESEARCH.md, D-17)
    - .planning/phases/02.1-migrate-supabase-to-neon-and-uploadthing/02.1-01-SUMMARY.md (verify Plan 01 fully replaced db client)
    - .planning/phases/02.1-migrate-supabase-to-neon-and-uploadthing/02.1-02-SUMMARY.md (verify Plan 02 wrote AUTH_SECRET path)
    - .planning/phases/02.1-migrate-supabase-to-neon-and-uploadthing/02.1-03-SUMMARY.md (verify Plan 03 wrote UPLOADTHING_TOKEN path)
    - src/types/env.d.ts (current declarations — what to remove)
    - .env.local.example (current placeholder doc — what to remove and rewrite)
  </read_first>
  <action>
    1. **Pre-flight grep guard.** Before any deletion, verify NO file in `src/` (other than the three doomed files in `src/lib/supabase/`) still imports from `@supabase/*`:
       ```bash
       grep -rn "from \"@supabase\|from '@supabase" src/ --exclude-dir=lib/supabase
       ```
       Expected output: zero matches. If matches exist, STOP and refactor those imports first (likely the executor missed an earlier step). Plans 01–03 are designed to leave `lib/supabase/*.ts` as the only Supabase consumers.

    2. **Uninstall Supabase packages:**
       ```bash
       npm uninstall @supabase/supabase-js @supabase/ssr
       ```
       Verify removal:
       ```bash
       grep -E "\"@supabase/(supabase-js|ssr)\":" package.json
       ```
       Expected: zero matches.

    3. **Delete the three Supabase client files** (D-04):
       ```bash
       rm src/lib/supabase/admin.ts
       rm src/lib/supabase/client.ts
       rm src/lib/supabase/server.ts
       rmdir src/lib/supabase  # remove the directory if empty
       ```
       If `rmdir` fails because there are other files, leave the directory but ensure the three .ts files are gone.

    4. **Rewrite `src/types/env.d.ts`** to match the exact shape in `<interfaces>` above. Preserve the namespace pattern. Sort keys: public block first, server-only block second, Node block last (matches existing convention from Phase 1).

    5. **Rewrite `.env.local.example`** to mirror env.d.ts. Use the existing comment-block style (────────) for visual organization. Group by service:
       ```
       # ────────────────────────────────────────────────
       # Neon — Dashboard > Connection Details
       # ────────────────────────────────────────────────
       # Pooled URL (hostname contains '-pooler') — used at runtime by lib/db
       DATABASE_URL=postgres://your-pooler-url-here
       # Direct URL (NO '-pooler' in hostname) — used by drizzle-kit push only
       DIRECT_DATABASE_URL=postgres://your-direct-url-here

       # ────────────────────────────────────────────────
       # NextAuth — generate with `npx auth secret`
       # ────────────────────────────────────────────────
       AUTH_SECRET=replace_with_32_random_bytes
       # AUTH_URL=https://your-canonical-app-url  # optional — auto-detected on Vercel

       # ────────────────────────────────────────────────
       # Uploadthing — Dashboard > API Keys > V7 tab
       # ────────────────────────────────────────────────
       UPLOADTHING_TOKEN=your_v7_token_here

       # ────────────────────────────────────────────────
       # Paystack — Dashboard > Settings > API Keys & Webhooks
       # ────────────────────────────────────────────────
       NEXT_PUBLIC_PAYSTACK_PUBLIC_KEY=pk_test_your_public_key
       PAYSTACK_SECRET_KEY=sk_test_your_secret_key
       PAYSTACK_WEBHOOK_SECRET=your_webhook_signing_secret

       # ────────────────────────────────────────────────
       # Resend — resend.com > API Keys
       # ────────────────────────────────────────────────
       RESEND_API_KEY=re_your_api_key
       RESEND_FROM_EMAIL=orders@rodoandco.com
       ADMIN_NOTIFICATION_EMAIL=admin@rodoandco.com

       # ────────────────────────────────────────────────
       # Admin seed (seed-time only — used by `npm run seed:admin`)
       # ────────────────────────────────────────────────
       # Minimum complexity: ≥ 12 chars, mixed case + digits + symbols
       ADMIN_EMAIL=admin@rodoandco.com
       ADMIN_PASSWORD=replace_with_strong_password

       # ────────────────────────────────────────────────
       # Vercel Cron — any strong random string
       # ────────────────────────────────────────────────
       CRON_SECRET=replace_with_strong_random_value

       # ────────────────────────────────────────────────
       # App
       # ────────────────────────────────────────────────
       NEXT_PUBLIC_APP_URL=http://localhost:3000
       ```
       Remove the entire Supabase block (lines under `# Supabase — Dashboard > Settings > API`).

    6. **Run guards:**
       - `grep -r "supabase" src/` — must return zero matches (case-sensitive — does not match Drizzle/etc.)
       - `grep -ri "supabase" src/types/env.d.ts .env.local.example` — must return zero matches
       - `npx tsc --noEmit` — must exit 0
       - `npm run build` — must exit 0 (this is the final gate; if any stale Supabase import slipped through, build fails here)

    Per D-15, D-16 (corrected to v5/v7 names), D-17: This task implements all three together because they are inseparable — env.d.ts and .env.local.example must agree.
  </action>
  <verify>
    <automated>
      ! grep -E "\"@supabase/(supabase-js|ssr)\":" package.json &amp;&amp;
      ! ls src/lib/supabase/admin.ts 2>/dev/null &amp;&amp;
      ! ls src/lib/supabase/client.ts 2>/dev/null &amp;&amp;
      ! ls src/lib/supabase/server.ts 2>/dev/null &amp;&amp;
      ! grep -r "supabase" src/ &amp;&amp;
      grep -q "DATABASE_URL: string" src/types/env.d.ts &amp;&amp;
      grep -q "AUTH_SECRET: string" src/types/env.d.ts &amp;&amp;
      grep -q "UPLOADTHING_TOKEN: string" src/types/env.d.ts &amp;&amp;
      ! grep -q "SUPABASE" src/types/env.d.ts &amp;&amp;
      grep -q "DATABASE_URL=" .env.local.example &amp;&amp;
      grep -q "AUTH_SECRET=" .env.local.example &amp;&amp;
      grep -q "UPLOADTHING_TOKEN=" .env.local.example &amp;&amp;
      ! grep -q "SUPABASE" .env.local.example &amp;&amp;
      ! grep -q "UPLOADTHING_SECRET\|UPLOADTHING_APP_ID\|NEXTAUTH_SECRET" .env.local.example &amp;&amp;
      ! grep -q "UPLOADTHING_SECRET\|UPLOADTHING_APP_ID\|NEXTAUTH_SECRET" src/types/env.d.ts &amp;&amp;
      npx tsc --noEmit &amp;&amp;
      npm run build
    </automated>
  </verify>
  <acceptance_criteria>
    - `package.json` has zero `@supabase/*` dependencies
    - `src/lib/supabase/admin.ts`, `client.ts`, `server.ts` are all deleted
    - `grep -r "supabase" src/` returns zero matches (no stale imports anywhere)
    - `src/types/env.d.ts` declares: `DATABASE_URL`, `DIRECT_DATABASE_URL`, `AUTH_SECRET`, `UPLOADTHING_TOKEN` (and keeps PAYSTACK_*, RESEND_*, ADMIN_*, CRON_SECRET, NEXT_PUBLIC_*)
    - `src/types/env.d.ts` does NOT declare `SUPABASE_*`, `NEXTAUTH_SECRET`, `UPLOADTHING_SECRET`, `UPLOADTHING_APP_ID` (the wrong v4/v6 names)
    - `.env.local.example` mirrors env.d.ts — same vars present, organized with comment blocks
    - `npx tsc --noEmit` exits 0
    - `npm run build` exits 0 — this is the final integration gate
  </acceptance_criteria>
  <done>
    Migration is clean. No Supabase code, dependencies, or env vars remain. Env contract is documented and accurate. The app builds end-to-end against Neon + NextAuth + Uploadthing.
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Source tree ⇄ Build output | Stale Supabase code or stale env declarations could cause runtime crashes or expose mismatched env contract |
| .env.local.example ⇄ developer onboarding | Misleading or wrong env names produce silent failures (Pitfall 2 + Pitfall 6) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02.1-17 | Tampering | Stale @supabase imports survive after refactor | mitigate | Pre-flight grep guard; final `npm run build` gate — TypeScript will fail on missing module imports if any survived |
| T-02.1-18 | Information Disclosure | env.d.ts still declares SUPABASE_SERVICE_ROLE_KEY but .env.local.example documents it (or vice versa) | mitigate | Acceptance criteria require BOTH files to match; `grep -q "SUPABASE"` in either file is a hard fail |
| T-02.1-19 | Tampering | Wrong env var names (NEXTAUTH_SECRET, UPLOADTHING_SECRET) silently accepted by env.d.ts | mitigate | Acceptance criteria explicitly forbid v4/v6 names — any `grep -q` for those names must fail |
| T-02.1-20 | Information Disclosure | Removed env vars still set in production .env (Vercel) | accept | This plan only touches the local repo; Vercel env vars are managed by the developer and will be cleaned up at deploy time. Phase 6 (launch hardening) explicitly checks production env contract. |

</threat_model>

<verification>
After this plan:
- `npm run build` exits 0 (final gate — passes only if every Plan 01–04 deliverable is consistent)
- `grep -r "supabase" src/` returns zero matches
- `grep -ri "supabase\|@supabase" src/ package.json` returns zero matches
- `diff <(grep -oE '\b(DATABASE_URL|DIRECT_DATABASE_URL|AUTH_SECRET|UPLOADTHING_TOKEN|PAYSTACK_SECRET_KEY|PAYSTACK_WEBHOOK_SECRET|RESEND_API_KEY|RESEND_FROM_EMAIL|ADMIN_NOTIFICATION_EMAIL|ADMIN_EMAIL|ADMIN_PASSWORD|CRON_SECRET|NEXT_PUBLIC_PAYSTACK_PUBLIC_KEY|NEXT_PUBLIC_APP_URL)\b' src/types/env.d.ts | sort -u) <(grep -oE '^[A-Z_]+=' .env.local.example | tr -d '=' | sort -u)` shows the two files declare overlapping vars (some env-only-at-runtime keys exist on both sides — this is informational, not a hard gate)
</verification>

<success_criteria>
1. Zero `@supabase/*` packages in package.json
2. Zero `src/lib/supabase/*.ts` files exist
3. Zero `supabase` references in `src/`
4. `src/types/env.d.ts` contains exactly the post-migration env contract (DATABASE_URL, DIRECT_DATABASE_URL, AUTH_SECRET, UPLOADTHING_TOKEN, plus retained vars)
5. `.env.local.example` mirrors env.d.ts with grouped, commented sections
6. No v4/v6 env names (`NEXTAUTH_SECRET`, `UPLOADTHING_SECRET`, `UPLOADTHING_APP_ID`) anywhere in the repo
7. `npm run build` exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/02.1-migrate-supabase-to-neon-and-uploadthing/02.1-04-SUMMARY.md`
</output>
