---
phase: 02.1-migrate-supabase-to-neon-and-uploadthing
verified: 2026-04-28T23:10:00Z
status: human_needed
score: 3/4 must-haves verified
overrides_applied: 0
re_verification:
  previous_status: gaps_found
  previous_score: 2/4
  gaps_closed:
    - "NextAuth v5 Credentials admin auth is enforced for all /admin/* routes (middleware guard is secure)"
  gaps_remaining: []
  regressions: []
human_verification:
  - test: "Neon schema pushed live and reachable via DATABASE_URL"
    expected: "Running scripts/smoke-test-db.ts exits 0 and prints OK for all 7 tables; ordering_config row 1 insert is idempotent"
    why_human: "Requires live Neon credentials and executing the script against the real database"
  - test: "NextAuth v5 Credentials end-to-end session creation + middleware behavior"
    expected: "Valid credentials create a signed session token; invalid credentials do not; /admin/* remains inaccessible without a valid token; valid token allows access"
    why_human: "Requires running the app and making real /api/auth requests; static inspection cannot prove runtime cookie issuance/verification"
  - test: "Uploadthing v7 upload with authenticated session"
    expected: "Unauthenticated POST is rejected; authenticated admin session can upload <=4MB image to productImage and receive a CDN URL"
    why_human: "Requires a running server, valid UPLOADTHING_TOKEN, and an authenticated browser/session"
requirements_notes:
  - "ROADMAP/PLAN use D-01..D-17 as requirements, but .planning/REQUIREMENTS.md does not define these IDs; verification cross-reference is therefore based on ROADMAP + plan frontmatter + code evidence."
---

# Phase 02.1: Migrate Supabase to Neon and Uploadthing — Verification Report (Re-run)

**Phase Goal (ROADMAP):** The app's infrastructure layer is fully migrated — Neon Postgres replaces Supabase PostgreSQL (all 7 tables in Drizzle schema-as-code, pushed live), NextAuth v5 Credentials provider replaces Supabase Auth (admin-only JWT sessions, middleware guards `/admin/*`), and Uploadthing v7 replaces Supabase Storage (productImage endpoint, auth-gated, typed client helpers ready for Phase 4). No customer-facing features change.
**Verified:** 2026-04-28T23:10:00Z
**Status:** human_needed
**Re-verification:** Yes — after `/admin/*` middleware guard fix

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Neon + Drizzle foundation exists (schema-as-code, db client is server-only, and schema can be pushed/tested) | ? UNCERTAIN | Drizzle schema + config exist (`drizzle/schema.ts`, `drizzle.config.ts`), server-only db module exists (`src/lib/db/index.ts`), and smoke/push scripts exist — but confirming schema is pushed live requires running `scripts/smoke-test-db.ts` against real Neon. |
| 2 | `/admin/*` is guarded by **cryptographically verified** NextAuth JWT/session (not cookie-name presence) | ✓ VERIFIED | `src/middleware.ts` uses `getToken({ req, secret: process.env.AUTH_SECRET })` and redirects when token verification fails. Repo-wide search shows no remaining `cookies.has("authjs.session-token")`-style gate in `src/`. |
| 3 | Uploadthing v7 is wired with a `productImage` endpoint (image, 4MB, 1 file), auth-gated via `await auth()`, and typed client helpers exist | ✓ VERIFIED | `src/app/api/uploadthing/core.ts` calls `await auth()` and rejects unauthenticated via `UploadThingError("Unauthorized")`; config enforces `maxFileSize: "4MB"` and `maxFileCount: 1`. `src/utils/uploadthing.ts` exports typed `UploadButton` / `UploadDropzone`. `src/app/layout.tsx` wires `NextSSRPlugin` with `extractRouterConfig(ourFileRouter)`. |
| 4 | Supabase is removed and env contract reflects Neon + NextAuth v5 + Uploadthing v7 | ✓ VERIFIED | `package.json` has no `@supabase/*` deps; `src/lib/supabase/` is absent; `src/` has no `supabase` references; `.env.local.example` + `src/types/env.d.ts` declare `DATABASE_URL`, `DIRECT_DATABASE_URL`, `AUTH_SECRET`, `UPLOADTHING_TOKEN` and do not include Supabase or legacy v4/v6 env names. |

**Score:** 3/4 truths verified

### Required Artifacts (Existence / Substantive / Wired)

| Artifact | Expected | Status | Details |
|--------|----------|--------|---------|
| `drizzle/schema.ts` | 7 tables (incl `admins`) matching legacy SQL columns | ✓ VERIFIED | Table definitions present and substantive. |
| `src/lib/db/index.ts` | Server-only Drizzle db via Neon HTTP driver | ✓ VERIFIED | `import "server-only";` + Neon HTTP + Drizzle. |
| `drizzle.config.ts` | Drizzle-kit config using `DIRECT_DATABASE_URL` | ✓ VERIFIED | `DIRECT_DATABASE_URL` referenced for DDL push. |
| `src/auth.ts` | NextAuth v5 Credentials provider backed by `admins` table | ✓ VERIFIED | Credentials provider queries `admins` and verifies bcrypt hash; `session: { strategy: "jwt" }`. |
| `src/app/api/auth/[...nextauth]/route.ts` | NextAuth route handler | ✓ VERIFIED | Re-exports `{ GET, POST }` from `handlers`. |
| `src/middleware.ts` | `/admin/*` guard | ✓ VERIFIED | Uses `getToken()` (verified JWT) rather than cookie-name presence. |
| `src/app/api/uploadthing/core.ts` | Uploadthing router with auth-gated middleware | ✓ VERIFIED | `await auth()` in middleware + Unauthorized error + `productImage` constraints. |
| `src/app/api/uploadthing/route.ts` | Uploadthing route handler | ✓ VERIFIED | `createRouteHandler({ router: ourFileRouter })` exports `{ GET, POST }`. |
| `src/utils/uploadthing.ts` | Typed Uploadthing React helpers | ✓ VERIFIED | Typed helpers exported. |
| `src/app/layout.tsx` | SSR plugin configured | ✓ VERIFIED | `NextSSRPlugin` present with `extractRouterConfig(ourFileRouter)`. |
| `.env.local.example` + `src/types/env.d.ts` | Accurate post-migration env contract | ✓ VERIFIED | Neon/NextAuth/Uploadthing envs declared; Supabase removed. |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `src/middleware.ts` | NextAuth JWT verification | `getToken({ req, secret: process.env.AUTH_SECRET })` | ✓ WIRED | Cryptographically verifies token signature using `AUTH_SECRET`. |
| `src/app/api/auth/[...nextauth]/route.ts` | `src/auth.ts` | `import { handlers } from "@/auth"` | ✓ WIRED | Present. |
| `src/auth.ts` | DB + admins table | `db.select().from(admins)...` | ✓ WIRED | Present (dynamic imports). |
| `src/app/api/uploadthing/core.ts` | NextAuth session | `await auth()` | ✓ WIRED | Present. |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|---------------|--------|-------------------|--------|
| `src/middleware.ts` | `token` | `getToken(...)` | ✓ Yes (if cookies + AUTH_SECRET configured) | ✓ FLOWING (cryptographic verification present; runtime depends on actual session issuance). |
| `src/auth.ts` | `admin` row | `db.select().from(admins)...` | ✓ Yes | ✓ FLOWING (query present; runtime depends on Neon seed). |
| `src/app/api/uploadthing/core.ts` | `session` | `await auth()` | ✓ Yes | ✓ FLOWING (auth() call present; runtime depends on NextAuth runtime config). |

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `src/app/admin/page.tsx` | 1-8 | Placeholder admin content | ℹ️ Info | Expected (Phase 4), but increases reliance on middleware for future admin pages. |

## Human Verification Required

### 1. Neon schema live + smoke test

**Test:** Run `npx tsx scripts/smoke-test-db.ts`  
**Expected:** Exits 0 and prints OK for all 7 tables; ordering_config seed is idempotent  
**Why human:** Requires live DB credentials and execution against Neon

### 2. NextAuth Credentials session issuance + `/admin/*` behavior

**Test:** Run the app and verify credential login creates a valid signed session; verify `/admin/orders` redirects when logged out and allows when logged in  
**Expected:** Without session, redirect to `/admin`; with valid session JWT, middleware allows through  
**Why human:** Requires runtime `/api/auth/*` flow + cookie issuance; cannot be proven by static inspection alone

### 3. Uploadthing authenticated upload

**Test:** With a valid admin session, upload an image <= 4MB via `productImage` endpoint  
**Expected:** Unauthed upload rejected; authed upload succeeds and returns a CDN URL  
**Why human:** Requires UPLOADTHING_TOKEN + running server + authenticated browser session

---

_Verified: 2026-04-28T23:10:00Z_  
_Verifier: Claude (gsd-verifier)_  
