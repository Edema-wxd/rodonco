---
phase: 02.1-migrate-supabase-to-neon-and-uploadthing
plan: 04
subsystem: cleanup
tags: [neon, drizzle, nextauth, uploadthing, env, cleanup]

requires:
  - phase: 02.1-01
    provides: Neon + Drizzle foundation (DATABASE_URL / DIRECT_DATABASE_URL)
  - phase: 02.1-02
    provides: NextAuth v5 (AUTH_SECRET)
  - phase: 02.1-03
    provides: Uploadthing v7 (UPLOADTHING_TOKEN)
provides:
  - No remaining Supabase dependencies or client code
  - Updated env var contract for post-migration stack
affects: [build, developer-onboarding]

tech-stack:
  removed: ["@supabase/supabase-js", "@supabase/ssr"]
  added: ["ws", "@types/ws"]
  patterns:
    - "Env contract matches Auth.js v5 (AUTH_SECRET) and Uploadthing v7 (UPLOADTHING_TOKEN)"

key-files:
  deleted:
    - src/lib/supabase/admin.ts
    - src/lib/supabase/client.ts
    - src/lib/supabase/server.ts
  modified:
    - package.json
    - package-lock.json
    - src/types/env.d.ts
    - .env.local.example

requirements-completed: [D-04, D-15, D-16, D-17]
completed: 2026-04-28
---

# Phase 02.1: migrate-supabase-to-neon-and-uploadthing — Plan 04 Summary

**Supabase is fully removed (deps + client code), and the repo’s env contract is updated to the post-migration set for Neon + NextAuth v5 + Uploadthing v7.**

## Accomplishments

- Uninstalled `@supabase/supabase-js` and `@supabase/ssr` and verified no remaining `@supabase/*` usage in `src/`.
- Deleted all Supabase client files and removed the now-empty `src/lib/supabase/` directory.
- Updated `src/types/env.d.ts` and `.env.local.example` to the new env var contract:
  - Added: `DATABASE_URL`, `DIRECT_DATABASE_URL`, `AUTH_SECRET`, `AUTH_URL?`, `UPLOADTHING_TOKEN`, plus seed-time `ADMIN_EMAIL`/`ADMIN_PASSWORD`
  - Removed: `NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_ANON_KEY`, `SUPABASE_SERVICE_ROLE_KEY`

## Verification

- `npx tsc --noEmit` passes
- `npm run build` passes
- `grep -r "supabase" src/` returns zero matches

## Task Commits

1. **Task 1: Uninstall Supabase deps, delete supabase clients, update env contract; run tsc + build** — `892db2e`

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Add `ws` + `@types/ws` so `scripts/push-schema.ts` typechecks**
- **Issue:** `npx tsc --noEmit` failed with `TS2307: Cannot find module 'ws'` due to an existing `scripts/push-schema.ts` import.
- **Fix:** Added `ws` and `@types/ws` as dev dependencies.
- **Commit:** `892db2e`

## Self-Check

PASSED

- Found SUMMARY file
- Found commit: `892db2e`

