---
phase: 03-interactive-shop
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - vitest.config.ts
  - src/test/setup.ts
  - src/store/cart.ts
  - src/store/cart.test.ts
autonomous: true
requirements:
  - CART-01
  - CART-02
must_haves:
  truths:
    - "Cart item identity is consistent across add/update/remove (product + variant/size + prep)"
    - "Cart persists via zustand/persist and remains serializable"
  artifacts:
    - path: "src/store/cart.ts"
      provides: "Cart store with consistent compound-key semantics"
    - path: "src/store/cart.test.ts"
      provides: "Automated regression coverage for keying + quantity updates"
    - path: "package.json"
      provides: "test script for automated verification"
  key_links:
    - from: "src/store/cart.ts"
      to: "src/types/index.ts"
      via: "CartItem.prepOption + CartItem.variantLabel"
      pattern: "prepOption|variantLabel"
    - from: "src/store/cart.test.ts"
      to: "src/store/cart.ts"
      via: "unit tests"
      pattern: "useCartStore"
---

<objective>
Make cart mutations correct and testable: align `removeItem`/`updateQuantity` keying with `addItem` merge semantics (must include `prepOption`), and add a fast automated test harness to prevent regressions.

Purpose: This phase relies on prep/size selections; incorrect cart keying will remove/update the wrong line items once options exist.
Output: Vitest test harness + regression tests + fixed cart store API.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-interactive-shop/03-CONTEXT.md
@src/store/cart.ts
@src/types/index.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Vitest harness and cart key regression tests</name>
  <files>
package.json
vitest.config.ts
src/test/setup.ts
src/store/cart.test.ts
  </files>
  <read_first>
    - Read `package.json` to add `test`/`test:watch` scripts without breaking existing scripts.
    - Read `src/store/cart.ts` and `src/types/index.ts` to mirror the current `CartItem` contract in tests.
  </read_first>
  <action>
    - Add dev dependencies for Vitest + jsdom + React testing utilities as needed for a Zustand store unit test environment.
    - Add `scripts.test` that runs quickly (e.g. `vitest run`) and does not require `next dev`.
    - Create `vitest.config.ts` with `environment: "jsdom"` and `setupFiles` pointing at `src/test/setup.ts`.
    - Write `src/store/cart.test.ts` covering:
      - Merge-on-add uses compound key (productId + variantLabel + prepOption) and increments quantity/subtotal correctly.
      - `updateQuantity` updates only the matching compound-key line item.
      - `removeItem` removes only the matching compound-key line item.
      - Qty <= 0 removal respects the compound key (no collateral removal).
  </action>
  <verify>
    <automated>npm test && npx tsc --noEmit && npm run build</automated>
  </verify>
  <done>
    - `npm test` runs and reports passing tests.
    - Tests explicitly cover at least two items sharing product+variant but different prepOption.
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix cart store API to include prepOption in identity</name>
  <files>src/store/cart.ts</files>
  <read_first>
    - Read `src/store/cart.ts` to ensure the fix matches existing merge semantics.
    - Read `src/types/index.ts` to confirm `CartItem.prepOption` exists and is nullable.
  </read_first>
  <action>
    - Update `removeItem` and `updateQuantity` signatures to include `prepOption: string | null`.
    - Update the internal filters/maps to match the `addItem` merge key exactly:
      `(productId, variantLabel, prepOption)`.
    - Keep `qty <= 0` behavior as “remove”, but apply it only to the matching compound key.
    - Ensure subtotal math remains integer kobo: `subtotalNgn = quantity * unitPriceNgn`.
  </action>
  <verify>
    <automated>npm test && npx tsc --noEmit && npm run build</automated>
  </verify>
  <done>
    - All tests added in Task 1 pass.
    - No cart operation can affect a different prepOption line item for the same product+variant.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client state → UI actions | User can trigger cart mutations arbitrarily; state transitions must remain safe and deterministic |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|----------|----------|-----------|-------------|-----------------|
| T-03-01 | T (Tampering) | `src/store/cart.ts` | mitigate | Use compound key consistently across add/update/remove; regression tests prevent accidental behavior changes |
| T-03-02 | D (DoS) | cart quantity | accept | Soft warning >20 is handled in UI later; store remains unbounded by decision (D-09) |
</threat_model>

<verification>
Run `npm test` locally and ensure it completes in under ~30s.
</verification>

<success_criteria>
- Cart compound-key semantics are correct and guarded by automated tests.
</success_criteria>

<output>
After completion, create `.planning/phases/03-interactive-shop/03-01-SUMMARY.md`
</output>

