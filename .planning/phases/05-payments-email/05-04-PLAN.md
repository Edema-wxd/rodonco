---
phase: 05-payments-email
plan: 04
type: execute
wave: 3
depends_on:
  - 05-03
files_modified:
  - src/app/api/paystack/webhook/route.ts
  - src/lib/paystack/verifySignature.ts
  - src/lib/paystack/verifySignature.test.ts
  - src/lib/email/resendClient.ts
  - src/lib/email/sendOrderEmails.ts
  - src/lib/email/templates/CustomerOrderReceipt.tsx
  - src/lib/email/templates/AdminNewOrderAlert.tsx
autonomous: true
requirements:
  - CHKT-06
  - CHKT-07
  - NOTF-02
  - NOTF-03
must_haves:
  truths:
    - "`req.text()` before JSON parse; HMAC-SHA512 verified with secret key (see 05-RESEARCH)"
    - "charge.success path sets order `paid` idempotently using `orders.reference`"
    - "Resend sends customer + admin emails; failures logged; handler still returns 200"
    - "Handler responds within Paystack guidance (target <5s)"
---

<objective>

Implement **`POST /api/paystack/webhook`**: raw-body HMAC verification, parse `charge.success`, idempotent transition **`pending` → `paid`**, then fire **Resend** React Email templates (customer receipt + admin alert) with fail-open logging (CONTEXT D-15).

Purpose: CHKT-06/07 + NOTF-02/03 — money path + notifications.

</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-payments-email/05-CONTEXT.md
@.planning/phases/05-payments-email/05-RESEARCH.md
@drizzle/schema.ts
@.env.local.example
</context>

<threat_model>

| Threat | Severity | Mitigation |
|--------|-----------|------------|
| Forged webhook | Critical | HMAC verify + ignore unsigned events |
| Replay | High | Idempotent update on `reference` + status check |
| Email exhaustion | Medium | No retry loop that blocks response; log only |

</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Signature helper + unit tests</name>
  <files>src/lib/paystack/verifySignature.ts, src/lib/paystack/verifySignature.test.ts</files>
  <verify>npm run test -- src/lib/paystack/verifySignature</verify>
</task>

<task type="auto" tdd="false">
  <name>Resend plumbing + templates</name>
  <files>src/lib/email/*</files>
  <action>Use `RESEND_API_KEY`, `RESEND_FROM_EMAIL`, `ADMIN_NOTIFICATION_EMAIL`.</action>
  <verify>npx tsc --noEmit</verify>
</task>

<task type="auto" tdd="false">
  <name>Webhook route</name>
  <files>src/app/api/paystack/webhook/route.ts</files>
  <action>Minimal JSON shaping for Paystack payloads; widen only as needed.</action>
  <verify>npm run test (plus manual curl vector optional)</verify>
</task>

</tasks>

<verification>`npm run test` + `npx tsc --noEmit`</verification>

<output>Create `05-04-SUMMARY.md` after execution.</output>
