---
phase: 09-tech-debt-cache-revalidation-requirements-cleanup
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
autonomous: true
requirements:
  - REQ-CLEANUP-09
tags:
  - documentation
  - requirements
  - traceability

must_haves:
  truths:
    - "Every requirement that was actually shipped (Phases 1-8) is checked `[x]` in REQUIREMENTS.md"
    - "FOUND-01 through FOUND-05 describe the actual shipped stack (Neon + Drizzle + NextAuth v5 + Uploadthing) — no Supabase references remain"
    - "REQUIREMENTS.md contains an `### Admin Operations` section with ADMIN-OPS IDs covering the Phase 7-8 features (prep list, manifest, pending orders, bulk transitions, week picker, activity log)"
    - "The traceability table lists every requirement against the phase that shipped it, including Phase 7 and Phase 8 rows"
    - "The traceability `Coverage:` summary count matches the actual number of v1 requirements after the audit"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "Audited requirements file with shipped items ticked, FOUND block rewritten, new ADMIN-OPS section, updated traceability"
      contains: "### Admin Operations"
      contains_also: "ADMIN-OPS-01"
  key_links:
    - from: ".planning/REQUIREMENTS.md FOUND-03"
      to: "drizzle/schema.ts table list"
      via: "actual table count and names match (no fictional '6 tables' or '7 tables' if schema has more)"
      pattern: "drizzle/schema\\.ts.*table"
    - from: ".planning/REQUIREMENTS.md FOUND-05"
      to: ".env.local.example variable list"
      via: "env var names listed in FOUND-05 are a subset of what actually appears in .env.local.example"
      pattern: "DATABASE_URL|AUTH_SECRET|UPLOADTHING_TOKEN|PAYSTACK"
    - from: ".planning/REQUIREMENTS.md Traceability table"
      to: ".planning/ROADMAP.md Phase entries"
      via: "Every requirement ID present in ROADMAP phase 'Requirements:' lines is also in the traceability table with matching phase"
      pattern: "Phase [1-8]"
---

<objective>
Reconcile `.planning/REQUIREMENTS.md` with the codebase as it actually shipped through Phase 8. The current REQUIREMENTS.md was authored on 2026-04-15 against a planned Supabase-based stack with 6 tables; the shipped reality is Neon + Drizzle + NextAuth v5 + Uploadthing with more tables, plus a substantial set of Phase 7 (route completeness) and Phase 8 (admin operations) features that were never captured as formal requirements.

Per D-07 (full audit) this plan does three docs-only things:
1. Tick every v1 requirement that was actually shipped as `[x]`
2. Rewrite FOUND-01 through FOUND-05 in place to describe the real stack (D-08), reading `drizzle/schema.ts` and `.env.local.example` as the sources of truth
3. Add a new `### Admin Operations` section with `ADMIN-OPS-XX` IDs (D-09) covering Phase 7-8 features, then extend the traceability table (D-10) so every requirement maps to its shipping phase

No schema changes, no code changes, no new pages. The only file touched is `.planning/REQUIREMENTS.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-tech-debt-cache-revalidation-requirements-cleanup/09-CONTEXT.md

@drizzle/schema.ts
@.env.local.example

<interfaces>
<!-- Source-of-truth facts the executor must use verbatim, derived from reading the files above. -->

drizzle/schema.ts declares the following pgTable exports (use this list, not the original "6 tables" or the D-08 hint of "7 tables" — read the schema and count):
- products
- product_variants
- product_prep_options
- orders
- order_items
- ordering_config
- product_images
- site_settings
- admins
- abandoned_carts
- activity_logs

Executor MUST run `grep -c '^export const .* = pgTable' drizzle/schema.ts` before writing FOUND-03 and use the exact integer that grep returns. Do NOT hardcode "7 tables" if the actual count differs — D-08's hint of "7" was approximate; the schema is the source of truth.

.env.local.example declares (executor MUST read the file directly; this is the snapshot at planning time — re-read to confirm):
- DATABASE_URL (pooled, runtime)
- DIRECT_DATABASE_URL (direct, drizzle-kit push)
- AUTH_SECRET (NextAuth v5 — note: not NEXTAUTH_SECRET)
- UPLOADTHING_TOKEN (v7)
- NEXT_PUBLIC_PAYSTACK_PUBLIC_KEY
- PAYSTACK_SECRET_KEY
- PAYSTACK_WEBHOOK_SECRET
- RESEND_API_KEY
- RESEND_FROM_EMAIL
- ADMIN_NOTIFICATION_EMAIL
- ADMIN_EMAIL (seed-time only)
- ADMIN_PASSWORD (seed-time only)
- CRON_SECRET
- NEXT_PUBLIC_APP_URL

ROADMAP.md Phase 7 requirements ID set (R-prefix): R1, R2, R3, R4, R5, R6
ROADMAP.md Phase 8 requirements ID set (OPS-prefix): OPS-01, OPS-02, OPS-03, OPS-04, OPS-05, OPS-06, OPS-07

Per D-09 the new ADMIN-OPS section should cover (executor decides exact wording and numbering per CONTEXT "Claude's Discretion"):
- Weekly prep/packing list (grouped by product/prep option) — corresponds to ROADMAP OPS-01
- Delivery manifest (itemised per customer) — corresponds to ROADMAP OPS-04
- Pending order visibility + delete — corresponds to ROADMAP OPS-06
- Bulk status transitions (paid → processing, etc.) — corresponds to ROADMAP OPS-07
- Analytics week picker — corresponds to ROADMAP OPS-02
- Activity log (admin action history) — observed in `drizzle/schema.ts` (activity_logs table) and `src/lib/admin/activityLog.ts` — was shipped but not in original REQUIREMENTS.md
- Customer search across name/phone/email — corresponds to ROADMAP OPS-03
- Settings: next_delivery_date + cutoff_message — corresponds to ROADMAP OPS-05

Current REQUIREMENTS.md status (read the file first to confirm) — the following items are still unchecked `[ ]` but shipped per ROADMAP phase completions:
- All FOUND-* (Phase 1 complete)
- All SHOP-05 through SHOP-11 (Phase 3 complete) — note SHOP-01 through SHOP-04 and CART-04 already `[x]`
- All CART-01, CART-02, CART-03, CART-05 (Phase 3 complete)
- All AUTH-*, ORD-*, PROD-*, ANLT-*, INFRA-03, INFRA-04 (Phase 4 complete)
- All CHKT-*, CONF-*, NOTF-02, NOTF-03 (Phase 5 complete)
- INFRA-01, INFRA-02, NOTF-01 (Phase 6 complete)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Audit and tick all shipped v1 requirements; rewrite FOUND-01 through FOUND-05</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (entire file — 199 lines — current state)
    - .planning/ROADMAP.md (Phase 1-6 entries for completed-status confirmation)
    - drizzle/schema.ts (entire file — 166 lines — source of truth for table list)
    - .env.local.example (entire file — 53 lines — source of truth for env var names)
    - .planning/phases/09-tech-debt-cache-revalidation-requirements-cleanup/09-CONTEXT.md (D-07, D-08)
  </read_first>
  <action>
    Open `.planning/REQUIREMENTS.md`. Perform the following edits in a single pass:

    1. **Tick shipped items.** For every requirement currently marked `[ ]` whose owning phase is marked Complete in ROADMAP.md (Phases 1, 2, 2.1, 3, 4, 5, 6 are all complete), change `[ ]` to `[x]`. This applies to: all FOUND-01..05; SHOP-05..11; CART-01, CART-02, CART-03, CART-05; AUTH-01..03; ORD-01..05; PROD-01..05; ANLT-01..04; INFRA-01..04; CHKT-01..07; CONF-01..03; NOTF-01..03. Leave SHOP-01..04 and CART-04 already-`[x]` items unchanged. Do NOT tick items in the `## v2 Requirements` section — those are explicitly future scope.

    2. **Rewrite FOUND-01.** Replace the existing text with: `Next.js 15 App Router project configured with TypeScript strict mode, Tailwind CSS v4, shadcn/ui, Zustand, React Hook Form, Zod (pinned to v3), and the `motion` library (the rebranded framer-motion).` Do NOT mention "Framer Motion" by its old name — per CONTEXT D-08 the project uses `motion`.

    3. **Rewrite FOUND-02.** Replace with: `Neon serverless Postgres connected via Drizzle ORM (lib/db.ts) and drizzle-kit for migrations; NextAuth v5 Credentials provider configured for admin auth (auth.ts + middleware.ts); Uploadthing v7 configured for product image storage (no Supabase clients exist).` This MUST explicitly say there are no Supabase clients (per D-08).

    4. **Rewrite FOUND-03.** Run `grep -c '^export const .* = pgTable' drizzle/schema.ts` to get the actual table count N. Replace FOUND-03 with: `All N database tables exist in Neon with the schema declared in drizzle/schema.ts: products, product_variants, product_prep_options, orders, order_items, ordering_config, product_images, site_settings, admins, abandoned_carts, activity_logs.` Substitute N with the integer grep returned. List every table name in the order they appear in drizzle/schema.ts. Do NOT say "RLS enabled" — Neon does not use Supabase RLS; the admin guard is at the NextAuth middleware layer.

    5. **Rewrite FOUND-04.** Replace with: `ordering_config row 1 seeded in Neon with `is_ordering_open = true` and `next_delivery_date` populated to the upcoming Saturday; admins table seeded via `npm run seed:admin`.` Do NOT mention Supabase seed scripts.

    6. **Rewrite FOUND-05.** Read `.env.local.example` line by line and list every env var name that appears outside a comment. Replace FOUND-05 with: `Environment variables documented in `.env.local.example` and confirmed not committed (.env.local in .gitignore): DATABASE_URL, DIRECT_DATABASE_URL, AUTH_SECRET, UPLOADTHING_TOKEN, NEXT_PUBLIC_PAYSTACK_PUBLIC_KEY, PAYSTACK_SECRET_KEY, PAYSTACK_WEBHOOK_SECRET, RESEND_API_KEY, RESEND_FROM_EMAIL, ADMIN_NOTIFICATION_EMAIL, ADMIN_EMAIL, ADMIN_PASSWORD, CRON_SECRET, NEXT_PUBLIC_APP_URL.` If the env file lists additional or different vars, use the actual contents — do not hallucinate variables that are not in the file.

    7. **Tick all five FOUND items** as `[x]` (Phase 1 shipped per ROADMAP).

    Do NOT modify the `## v2 Requirements`, `## Out of Scope`, or the file's top metadata header (lines 1-6) other than updating the `*Last updated:*` footer line at the very end to today's date `2026-05-22`.
  </action>
  <verify>
    <automated>grep -c '^- \[x\] \*\*FOUND-0[1-5]\*\*' .planning/REQUIREMENTS.md | awk '$1 == 5 {exit 0} {exit 1}' &amp;&amp; grep -q 'Neon' .planning/REQUIREMENTS.md &amp;&amp; grep -q 'Drizzle' .planning/REQUIREMENTS.md &amp;&amp; grep -q 'NextAuth' .planning/REQUIREMENTS.md &amp;&amp; grep -q 'Uploadthing' .planning/REQUIREMENTS.md &amp;&amp; ! grep -qi 'supabase' .planning/REQUIREMENTS.md &amp;&amp; grep -q 'AUTH_SECRET' .planning/REQUIREMENTS.md &amp;&amp; grep -q 'UPLOADTHING_TOKEN' .planning/REQUIREMENTS.md &amp;&amp; grep -q 'DATABASE_URL' .planning/REQUIREMENTS.md &amp;&amp; ! grep -qE '^- \[ \] \*\*(SHOP-0[5-9]|SHOP-1[01]|CART-0[1235]|AUTH-0[1-3]|ORD-0[1-5]|PROD-0[1-5]|ANLT-0[1-4]|INFRA-0[1-4]|CHKT-0[1-7]|CONF-0[1-3]|NOTF-0[1-3])\*\*' .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - All 5 FOUND-* items are ticked `[x]` (verify: `grep -c '^- \[x\] \*\*FOUND-0[1-5]\*\*'` returns exactly `5`)
    - The string "Neon" appears in REQUIREMENTS.md (replacing Supabase references in FOUND block)
    - The string "Supabase" does NOT appear anywhere in REQUIREMENTS.md, case-insensitive (`grep -i supabase .planning/REQUIREMENTS.md` returns no matches)
    - The strings "Drizzle", "NextAuth", "Uploadthing" all appear in REQUIREMENTS.md
    - FOUND-05 contains every env var name listed in `.env.local.example` (test by grep for DATABASE_URL, AUTH_SECRET, UPLOADTHING_TOKEN, PAYSTACK_SECRET_KEY, RESEND_API_KEY, CRON_SECRET — all must appear)
    - FOUND-03 contains the actual table count integer matching `grep -c '^export const .* = pgTable' drizzle/schema.ts` (executor records this number in the SUMMARY)
    - FOUND-03 mentions all 11 table names: products, product_variants, product_prep_options, orders, order_items, ordering_config, product_images, site_settings, admins, abandoned_carts, activity_logs
    - No v1 requirement from completed phases remains unchecked — verify: `grep -E '^- \[ \] \*\*(SHOP-0[5-9]|SHOP-1[01]|CART-0[1235]|AUTH-0[1-3]|ORD-0[1-5]|PROD-0[1-5]|ANLT-0[1-4]|INFRA-0[1-4]|CHKT-0[1-7]|CONF-0[1-3]|NOTF-0[1-3])\*\*'` returns zero lines
    - v2 Requirements section is untouched (still under `## v2 Requirements` heading, UX-01..03 and ADM-01..04 still without `[x]` ticks)
  </acceptance_criteria>
  <done>Every shipped v1 requirement is ticked, the FOUND block describes the real Neon/Drizzle/NextAuth/Uploadthing stack with no Supabase residue, and the env var list matches `.env.local.example`.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add `### Admin Operations` section with ADMIN-OPS IDs for Phase 7-8 features</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (re-read after Task 1's edits)
    - .planning/ROADMAP.md (Phase 7 §Requirements R1-R6, Phase 8 §Requirements OPS-01..07)
    - drizzle/schema.ts (confirm activity_logs and abandoned_carts tables — these correspond to ADMIN-OPS items)
    - .planning/phases/09-tech-debt-cache-revalidation-requirements-cleanup/09-CONTEXT.md (D-09)
  </read_first>
  <action>
    Insert a new `### Admin Operations` section into `.planning/REQUIREMENTS.md` immediately after the existing `### Infrastructure & Cutoff` block and BEFORE the `---` separator that introduces `## v2 Requirements`. Insert the section under the `## v1 Requirements` heading (it is part of v1 — these features shipped).

    The section MUST contain checklist items with `ADMIN-OPS-XX` IDs (zero-padded two-digit) covering every Phase 7-8 deliverable from ROADMAP. Each item MUST be ticked `[x]` because Phases 7 and 8 are both complete per ROADMAP. Use this exact set (executor may refine wording; IDs and coverage are mandatory):

    - `ADMIN-OPS-01`: Weekly prep/packing list — aggregate quantities per `product_name + variant_label + prep_option` across all `paid` and `processing` orders for a selected delivery week (Phase 8 OPS-01)
    - `ADMIN-OPS-02`: Delivery manifest — per-week printable view of customer name, phone, address, and order items (Phase 8 OPS-04)
    - `ADMIN-OPS-03`: Pending order visibility — abandoned/unpaid orders viewable in a separate admin view with count badge; admin can delete individual pending orders (Phase 8 OPS-06)
    - `ADMIN-OPS-04`: Bulk status transitions — move all `paid → processing` or all `processing → delivered` orders for a delivery week in one action (Phase 8 OPS-07)
    - `ADMIN-OPS-05`: Analytics week picker — analytics page accepts a `weekOverride` parameter wired from a UI picker so historical weeks are inspectable without code changes (Phase 8 OPS-02)
    - `ADMIN-OPS-06`: Customer search — orders table search field filters across `customer_name`, `customer_phone`, and `customer_email` in real time (Phase 8 OPS-03)
    - `ADMIN-OPS-07`: Settings — `next_delivery_date` and `cutoff_message` editable from the Settings page with immediate cutoff-banner reflection (Phase 8 OPS-05)
    - `ADMIN-OPS-08`: Activity log — admin actions (product create/update/delete, settings updates, bulk transitions) are recorded to the `activity_logs` table and viewable from the admin activity page

    Also add a new `### Route Completeness` section directly before `### Admin Operations` covering Phase 7's six requirements:

    - `ROUTES-01`: Global 404 page (`src/app/not-found.tsx`) renders with Navbar and Footer for any unmatched URL (Phase 7 R1)
    - `ROUTES-02`: Privacy Policy page at `/privacy` returns HTTP 200 (Phase 7 R2)
    - `ROUTES-03`: Terms of Service page at `/terms` returns HTTP 200 (Phase 7 R3)
    - `ROUTES-04`: Cookie Policy page at `/cookie-policy` returns HTTP 200 (Phase 7 R4)
    - `ROUTES-05`: Plans page at `/plans` returns HTTP 200; Navbar "Plans" link points to `/plans` (Phase 7 R5)
    - `ROUTES-06`: Footer contains no dead links — unbuilt marketing pages removed from `Footer.tsx` (Phase 7 R6)

    All ROUTES-* items must be ticked `[x]` (Phase 7 complete). All ADMIN-OPS-* items must be ticked `[x]` (Phase 8 complete). Do NOT touch any other section.
  </action>
  <verify>
    <automated>grep -c '^### Admin Operations' .planning/REQUIREMENTS.md | awk '$1 == 1 {exit 0} {exit 1}' &amp;&amp; grep -c '^### Route Completeness' .planning/REQUIREMENTS.md | awk '$1 == 1 {exit 0} {exit 1}' &amp;&amp; grep -c '\*\*ADMIN-OPS-0[1-8]\*\*' .planning/REQUIREMENTS.md | awk '$1 == 8 {exit 0} {exit 1}' &amp;&amp; grep -c '\*\*ROUTES-0[1-6]\*\*' .planning/REQUIREMENTS.md | awk '$1 == 6 {exit 0} {exit 1}' &amp;&amp; grep -c '^- \[x\] \*\*ADMIN-OPS-' .planning/REQUIREMENTS.md | awk '$1 == 8 {exit 0} {exit 1}' &amp;&amp; grep -c '^- \[x\] \*\*ROUTES-' .planning/REQUIREMENTS.md | awk '$1 == 6 {exit 0} {exit 1}'</automated>
  </verify>
  <acceptance_criteria>
    - REQUIREMENTS.md contains exactly one `### Admin Operations` heading
    - REQUIREMENTS.md contains exactly one `### Route Completeness` heading
    - 8 ADMIN-OPS items present and ticked `[x]`: ADMIN-OPS-01 through ADMIN-OPS-08
    - 6 ROUTES items present and ticked `[x]`: ROUTES-01 through ROUTES-06
    - Both new sections appear under `## v1 Requirements` (above the `---` that introduces `## v2 Requirements`)
    - The `## v2 Requirements`, `## Out of Scope`, and `## Traceability` sections are unmodified by this task (Task 3 handles traceability)
    - Each ADMIN-OPS item text references the corresponding Phase 8 OPS- ID from ROADMAP (e.g. "(Phase 8 OPS-01)" appears in ADMIN-OPS-01's bullet)
    - Each ROUTES item text references the corresponding Phase 7 R- ID
  </acceptance_criteria>
  <done>Phase 7 (Route Completeness) and Phase 8 (Admin Operations) features now have first-class requirement IDs in REQUIREMENTS.md and are ticked as shipped.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Extend traceability table with Phase 7, Phase 8, and recompute coverage totals</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (re-read after Tasks 1 + 2's edits)
    - .planning/ROADMAP.md (Phase 7 + Phase 8 Requirements lines)
    - .planning/phases/09-tech-debt-cache-revalidation-requirements-cleanup/09-CONTEXT.md (D-10)
  </read_first>
  <action>
    In the existing `## Traceability` section of REQUIREMENTS.md:

    1. **Append Phase 7 rows** to the table, one per ROUTES-* ID added in Task 2. All rows use `Phase 7` in the middle column and `Complete` in the Status column. Insert after the last Phase 6 row (`NOTF-01 | Phase 6 | Pending` — note: Task 1 should already have flipped this to `Complete` via the Status column update below).

    2. **Append Phase 8 rows** to the table, one per ADMIN-OPS-* ID. All rows use `Phase 8` and `Complete`.

    3. **Update every existing row's Status column** from `Pending` to `Complete` for requirements that were ticked in Task 1 (i.e. all Phase 1-6 rows since Phases 1, 2, 2.1, 3, 4, 5, 6 are all complete per ROADMAP). Do NOT change FOUND row phase assignments — they stay `Phase 1`.

    4. **Update the Coverage block** at the bottom:
       - Recount v1 requirements: count every line in `## v1 Requirements` matching `^- \[[ x]\] \*\*[A-Z]+-` (use `grep -cE` to get the integer). Call this number TOTAL.
       - Coverage block must read:
         ```
         **Coverage:**
         - v1 requirements: TOTAL total
         - Mapped to phases: TOTAL
         - Unmapped: 0 ✓
         ```
         Substitute TOTAL with the actual integer (it will be larger than 46 because of the new ADMIN-OPS-01..08 and ROUTES-01..06 entries — roughly 60).

    5. **Update the footer line** at the very bottom from `*Last updated: 2026-04-15 — traceability updated after roadmap creation*` to `*Last updated: 2026-05-22 — Phase 9 audit: FOUND block rewritten for Neon/Drizzle/NextAuth/Uploadthing stack, ADMIN-OPS + ROUTES sections added, traceability updated through Phase 8*`.

    Do NOT remove any existing rows from the traceability table. Do NOT renumber existing requirement IDs.
  </action>
  <verify>
    <automated>grep -cE '\| Phase 7 \| Complete' .planning/REQUIREMENTS.md | awk '$1 == 6 {exit 0} {exit 1}' &amp;&amp; grep -cE '\| Phase 8 \| Complete' .planning/REQUIREMENTS.md | awk '$1 == 8 {exit 0} {exit 1}' &amp;&amp; ! grep -qE '\| Phase [1-6] \| Pending' .planning/REQUIREMENTS.md &amp;&amp; grep -q '2026-05-22' .planning/REQUIREMENTS.md &amp;&amp; grep -q 'Unmapped: 0 ' .planning/REQUIREMENTS.md &amp;&amp; awk '/^## v1 Requirements/,/^---$/' .planning/REQUIREMENTS.md | grep -cE '^- \[[ x]\] \*\*[A-Z]+(-[A-Z]+)?-[0-9]+\*\*' &gt; /tmp/v1count &amp;&amp; V1=$(cat /tmp/v1count) &amp;&amp; grep -qE "v1 requirements: ${V1} total" .planning/REQUIREMENTS.md &amp;&amp; grep -qE "Mapped to phases: ${V1}" .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - Traceability table contains exactly 6 rows with `| Phase 7 | Complete` (one per ROUTES-01..06)
    - Traceability table contains exactly 8 rows with `| Phase 8 | Complete` (one per ADMIN-OPS-01..08)
    - Zero rows in the table contain `| Phase [1-6] | Pending` — every Phase 1-6 row's Status is now `Complete`
    - Footer line at file end contains the literal date `2026-05-22`
    - Coverage block: "v1 requirements: N total" and "Mapped to phases: N" use the same integer N, which equals the count of v1 requirement bullets in `## v1 Requirements` section
    - "Unmapped: 0 ✓" line is present
    - No existing requirement IDs were renamed or removed (regression-check by grepping for FOUND-01, SHOP-01, AUTH-01, CHKT-01 — all must still appear)
  </acceptance_criteria>
  <done>The traceability table covers every requirement (original v1 + new ROUTES + new ADMIN-OPS) against its shipping phase, every shipped row says Complete, and the coverage totals reflect the new count.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This plan touches only `.planning/REQUIREMENTS.md` — a project-internal documentation artifact under version control. No runtime code path is modified; no input crosses any trust boundary. The only "boundary" is the developer editing a tracked file.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-D-01 | Tampering | REQUIREMENTS.md content | accept | File is under git; any unintended change is recoverable via revert. Grep-based acceptance criteria catch accidental Supabase reintroduction or missed tick boxes |
| T-09-D-02 | Information Disclosure | Env var names in FOUND-05 | mitigate | FOUND-05 lists only env var NAMES (already public in `.env.local.example`, also committed). No actual secret values are written to the doc — executor must NOT paste values from `.env.local` |
| T-09-D-03 | Repudiation | Doc edits | accept | Git blame on REQUIREMENTS.md records the commit; the Phase 9 SUMMARY records the audit rationale |
| T-09-D-04 | Spoofing | Fictional requirement IDs | mitigate | All new IDs (ADMIN-OPS-01..08, ROUTES-01..06) map 1:1 to existing ROADMAP Phase 7-8 requirements that did ship — verify by cross-referencing ROADMAP.md before committing |
| T-09-D-05 | Tampering | Schema-doc drift | mitigate | FOUND-03 derives table count from `grep -c '^export const .* = pgTable' drizzle/schema.ts` at edit time. If schema later changes, REQUIREMENTS.md will drift — but that is a future concern, not a Phase 9 threat |
| T-09-D-SC | Tampering | npm/pip/cargo installs | N/A | This plan installs no packages — pure docs edit. Package legitimacy gate not triggered |
</threat_model>

<verification>
- `.planning/REQUIREMENTS.md` no longer contains the string "Supabase" (case-insensitive)
- All Phase 1-6 v1 requirements are ticked `[x]`
- New `### Admin Operations` section exists with 8 ADMIN-OPS items all `[x]`
- New `### Route Completeness` section exists with 6 ROUTES items all `[x]`
- Traceability table has 6 Phase-7 rows + 8 Phase-8 rows + Complete status on every existing row
- Coverage block total matches actual count of v1 requirement bullets
- File still parses as valid Markdown (open in a previewer or run a Markdown linter if available)
- `git diff .planning/REQUIREMENTS.md` shows only intentional changes — no whitespace churn outside edited sections
</verification>

<success_criteria>
- All three tasks pass their automated `<verify>` commands
- `grep -i supabase .planning/REQUIREMENTS.md` returns no matches
- `grep -c '^- \[ \] \*\*' .planning/REQUIREMENTS.md` (count of unchecked items) equals exactly the number of v2 requirements (UX-01..03 + ADM-01..04 = 7) — i.e. every v1 item is checked
- The integer in "v1 requirements: N total" matches the integer in "Mapped to phases: N"
- FOUND-03 lists exactly the table names declared as `pgTable` exports in `drizzle/schema.ts`
- FOUND-05 lists exactly the env var names declared (outside comments) in `.env.local.example`
</success_criteria>

<output>
Create `.planning/phases/09-tech-debt-cache-revalidation-requirements-cleanup/09-02-SUMMARY.md` when done. Summary must include:
- Actual table count integer used in FOUND-03 (and the grep command output that produced it)
- Total v1 requirement count after the audit (and the grep command that produced it)
- Confirmation that no "Supabase" reference remains
- List of new requirement IDs added (ADMIN-OPS-01..08 and ROUTES-01..06)
- Number of traceability rows added (expected: 14 — 6 Phase 7 + 8 Phase 8)
</output>
