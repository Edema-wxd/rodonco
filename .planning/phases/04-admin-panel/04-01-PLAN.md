---
phase: 04-admin-panel
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - src/components/ui/table.tsx
  - src/components/ui/sheet.tsx
  - src/components/ui/select.tsx
  - src/components/ui/badge.tsx
  - src/components/ui/switch.tsx
  - src/components/ui/input.tsx
  - src/components/ui/label.tsx
  - src/components/ui/separator.tsx
  - src/components/ui/card.tsx
  - src/components/ui/textarea.tsx
autonomous: true
requirements: []
must_haves:
  truths:
    - "All shadcn primitives required by the admin panel exist under src/components/ui/ and import without error"
  artifacts:
    - path: "src/components/ui/table.tsx"
      provides: "shadcn Table primitive (admin tables)"
    - path: "src/components/ui/sheet.tsx"
      provides: "shadcn Sheet primitive (product drawer)"
    - path: "src/components/ui/select.tsx"
      provides: "shadcn Select primitive (filters/status)"
    - path: "src/components/ui/badge.tsx"
      provides: "shadcn Badge primitive (status pills)"
    - path: "src/components/ui/switch.tsx"
      provides: "shadcn Switch primitive (is_active + ordering toggle)"
    - path: "src/components/ui/input.tsx"
      provides: "shadcn Input primitive (forms + date filter)"
    - path: "src/components/ui/label.tsx"
      provides: "shadcn Label primitive (forms)"
    - path: "src/components/ui/separator.tsx"
      provides: "shadcn Separator primitive (sidebar sections)"
    - path: "src/components/ui/card.tsx"
      provides: "shadcn Card primitive (analytics/settings)"
    - path: "src/components/ui/textarea.tsx"
      provides: "shadcn Textarea primitive (product description)"
---

<objective>
Install the shadcn/ui primitives required by `04-UI-SPEC.md` for the Phase 4 admin panel.

Purpose: unblock Wave 1 feature work (login, orders, products, analytics, settings) by ensuring all shared UI primitives exist under `src/components/ui/` and match the project’s existing shadcn export style.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-admin-panel/04-UI-SPEC.md
@src/components/ui/button.tsx
@package.json
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Install required shadcn primitives via CLI</name>
  <files>
    src/components/ui/table.tsx,
    src/components/ui/sheet.tsx,
    src/components/ui/select.tsx,
    src/components/ui/badge.tsx,
    src/components/ui/switch.tsx,
    src/components/ui/input.tsx,
    src/components/ui/label.tsx,
    src/components/ui/separator.tsx,
    src/components/ui/card.tsx,
    src/components/ui/textarea.tsx
  </files>
  <read_first>
    .planning/phases/04-admin-panel/04-UI-SPEC.md (install list),
    src/components/ui/button.tsx (existing shadcn style — match export conventions)
  </read_first>
  <action>
    Run the installs sequentially:
    ```bash
    npx shadcn@latest add table -y
    npx shadcn@latest add sheet -y
    npx shadcn@latest add select -y
    npx shadcn@latest add badge -y
    npx shadcn@latest add switch -y
    npx shadcn@latest add input -y
    npx shadcn@latest add label -y
    npx shadcn@latest add separator -y
    npx shadcn@latest add card -y
    npx shadcn@latest add textarea -y
    ```
    If the CLI proposes overwriting `src/components/ui/button.tsx`, decline (it already exists).
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `ls src/components/ui/{table,sheet,select,badge,switch,input,label,separator,card,textarea}.tsx` returns all 10 paths.
    - `grep -l "export" src/components/ui/{table,sheet,select,badge,switch,input,label,separator,card,textarea}.tsx | wc -l` returns 10.
  </acceptance_criteria>
  <done>All required shadcn primitives exist and typecheck cleanly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI → workspace | shadcn writes trusted UI primitives into `src/components/ui/*`. No runtime trust boundary crossed. |
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0.
- All 10 primitives exist under `src/components/ui/`.
</verification>

<success_criteria>
- All required shadcn primitives are installed via the CLI (not hand-written).
- No `src/app/admin/*` or `src/app/api/admin/*` files are changed in this plan.
</success_criteria>

<output>
After completion, create `.planning/phases/04-admin-panel/04-01-SUMMARY.md` documenting the final list of installed shadcn primitives.
</output>
