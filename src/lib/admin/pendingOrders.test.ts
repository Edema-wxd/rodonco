import { describe, it } from "vitest";

// Implementation pending — stubs only so suite is green
// The lib file src/lib/admin/pendingOrders.ts will be created in Wave 2.
// These tests will be filled in once the implementation exists.
describe("getPendingOrders", () => {
  it.todo("returns only status=pending orders");
});

describe("getPendingOrdersCount", () => {
  it.todo("returns numeric count of pending orders");
});
