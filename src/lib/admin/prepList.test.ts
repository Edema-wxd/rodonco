import { describe, it } from "vitest";

// Implementation pending — stubs only so suite is green
// The lib file src/lib/admin/prepList.ts will be created in Wave 2.
// These tests will be filled in once the implementation exists.
describe("getPrepList", () => {
  it.todo("aggregates quantities by product_name + variant_label + prep_option");
  it.todo("coerces sum() string to number");
  it.todo("filters status IN paid processing");
});
