import { describe, it } from "vitest";

// Implementation pending — stubs only so suite is green
// The lib file src/lib/admin/bulkTransition.ts will be created in Wave 2.
// These tests will be filled in once the implementation exists.
describe("bulkTransitionOrders", () => {
  it.todo("calls db.update with and(eq week_of, eq from_status)");
  it.todo("returns rowCount defensively as number");
});
